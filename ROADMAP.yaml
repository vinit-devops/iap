# Machine-readable roadmap tracker for the IIS ecosystem (roadmap §10, §17.3).
# Maintained by the executing agent: one milestone at a time, status + evidence
# updated as work completes. Phase completion is gated by exit criteria, never dates.
# statuses: pending | in-progress | completed | blocked
version: 1
updated: '2026-07-11' # Phases 3, 5, 10-18 completed — WHOLE ROADMAP COMPLETE
source: roadmap # the master implementation prompt at repo root

phases:
  - id: '0'
    title: Foundation and Repository Baseline
    priority: P0
    status: completed
    dependsOn: []
    milestones:
      - id: M0.1
        title: Monorepo restructure and workspace tooling
        status: completed
        evidence:
          - docs/milestones/M0.1-monorepo-foundation.md
          - package.json
          - .github/workflows/ci.yml
      - id: M0.2
        title: Governance files, ADR/IEP templates, execution protocol
        status: completed
        evidence:
          - docs/milestones/M0.2-governance.md
          - GOVERNANCE.md
          - docs/adr/ADR-0001-monorepo-and-primary-language.md
          - spec/ieps/IEP-TEMPLATE.md
      - id: M0.3
        title: v1 gap analysis report
        status: completed
        evidence:
          - docs/reports/v1-gap-analysis.md
          - docs/milestones/M0.3-gap-analysis.md
      - id: M0.4
        title: Machine-readable roadmap tracker
        status: completed
        evidence:
          - ROADMAP.yaml
      - id: M0.5
        title: Initial IEPs 0008-0013
        status: completed
        evidence:
          - docs/milestones/M0.5-ieps.md
          - spec/ieps/IEP-0008-canonical-infrastructure-model.md
          - spec/ieps/IEP-0013-ai-mcp-trust-boundaries.md
      - id: M0.6
        title: Minimum parser and canonical-model packages + automated spec validation
        status: completed
        evidence:
          - docs/milestones/M0.6-parser-model.md
          - packages/model/src/index.ts
          - packages/parser/src/index.ts
          - tests/conformance/run.mjs
    exitCriteria:
      - Clean checkout can install, build, lint, and test
      - Existing schemas and examples are validated automatically
      - Conformance suite runs via a single command (CI-ready)
      - Protected main-branch workflow is documented (staged; git deferred by user directive)
      - No implementation package contains provider-specific core types

  - id: '0.5'
    title: IIS Enhancement Proposal Process
    priority: P0
    status: completed
    dependsOn: ['0']
    milestones:
      - id: M05.1
        title: IEP template, index, lifecycle, review rules
        status: completed
        evidence:
          - spec/ieps/IEP-TEMPLATE.md
          - spec/ieps/README.md
      - id: M05.2
        title: IEPs 0008-0013 drafted
        status: completed
        evidence:
          - spec/ieps/IEP-0008-canonical-infrastructure-model.md
          - spec/ieps/IEP-0009-intent-compiler-operations.md
          - spec/ieps/IEP-0010-state-and-reconciliation.md
          - spec/ieps/IEP-0011-deterministic-planning-contract.md
          - spec/ieps/IEP-0012-provider-conformance.md
          - spec/ieps/IEP-0013-ai-mcp-trust-boundaries.md
      - id: M05.3
        title: Retroactive IEPs 0001-0007 formalizing existing v1 design
        status: completed
        evidence:
          - docs/milestones/M05.3-retroactive-ieps.md
          - spec/ieps/IEP-0001-resource-model.md
          - spec/ieps/IEP-0007-extension-framework.md
    exitCriteria:
      - No normative specification change merges without a required IEP
      - IEP status enforced through review policy
      - Every unresolved specification question is linked to an IEP or explicitly deferred

  - id: '1'
    title: IIS v1 Stabilization
    priority: P0
    status: completed
    dependsOn: ['0', '0.5']
    milestones:
      - id: M1.1
        title: Canonicalization precision (quantity normalization, default materialization)
        status: completed
        evidence:
          - docs/milestones/M1.1-canonicalization-precision.md
      - id: M1.2
        title: Non-breaking corrections from gap report §7
        status: completed
        evidence:
          - docs/milestones/M1.2-nonbreaking-corrections.md
      - id: M1.3
        title: Conformance coverage expansion (gap report §4 case list)
        status: completed
        evidence:
          - docs/milestones/M1.3-conformance-expansion.md
          - spec/conformance/cases/invalid/07-inert-deadletter.iis.yaml
          - spec/conformance/cases/invalid/22-postmerge-invalid.iis.yaml
      - id: M1.4
        title: Additional official examples (serverless, private internal, data-processing, hybrid, import intent)
        status: completed
        evidence:
          - docs/milestones/M1.4-additional-examples.md
          - spec/examples/serverless-api.iis.yaml
          - spec/examples/import-intent.iis.yaml
      - id: M1.5
        title: Error-code registry artifact + schema compatibility report
        status: completed
        evidence:
          - docs/milestones/M1.5-error-registry-compat.md
          - spec/conformance/error-codes.yaml
          - docs/reports/schema-compatibility-1.0.md
    exitCriteria:
      - No known contradiction between normative prose and schemas
      - All official examples pass
      - All invalid conformance examples fail predictably
      - Breaking changes require an accepted IEP
      - Core kind registry frozen for the implementation milestone

  - id: '2'
    title: Canonical Model and Reference SDK
    priority: P0
    status: completed
    dependsOn: ['1']
    milestones:
      - id: M2.1
        title: '@iis/parser — YAML/JSON, source locations, duplicate-key rejection'
        status: completed
        evidence:
          - docs/milestones/M2.1-parser-source-maps.md
          - packages/parser/src/source-map.ts
      - id: M2.2
        title: '@iis/model — Canonical Infrastructure Model types (IEP-0008)'
        status: completed
        evidence:
          - docs/milestones/M2.2-M2.3-cim-canonicalization.md
          - packages/model/src/cim.ts
      - id: M2.3
        title: Canonicalization engine + stable hashing + determinism suite
        status: completed
        evidence:
          - docs/milestones/M2.2-M2.3-cim-canonicalization.md
          - packages/model/src/canonicalize.ts
          - packages/model/src/quantity.ts
          - packages/model/test/canonicalize.test.ts
          - packages/model/test/quantity.test.ts
      - id: M2.4
        title: Schema + semantic validators (validation phases 1-4)
        status: completed
        evidence:
          - docs/milestones/M2.4-M2.5-validators-graph.md
          - packages/validator/src/index.ts
          - packages/validator/test/validator.test.ts
          - tests/conformance/run.mjs
      - id: M2.5
        title: Relationship + dependency engines (normalization, cycle detection, graph API)
        status: completed
        evidence:
          - docs/milestones/M2.4-M2.5-validators-graph.md
          - packages/graph/src/index.ts
          - packages/graph/test/graph.test.ts
      - id: M2.6
        title: SDK facade + plugin registration + round-trip serializer
        status: completed
        evidence:
          - docs/milestones/M2.6-sdk-facade.md
          - docs/reports/phase-2-completion.md
          - packages/sdk/src/index.ts
          - packages/sdk/test/sdk.test.ts
          - packages/sdk/test/api-surface.test.ts
    exitCriteria:
      - Every official example parses and validates
      - Parse -> canonicalize -> serialize -> parse produces an equivalent model
      - Identical inputs generate identical canonical hashes
      - Graph results independent of input key ordering
      - No provider SDK required to parse core IIS
      - Public APIs have compatibility tests

  - id: '3'
    title: Intent Authoring Engine and Intent Compiler
    priority: P1
    status: completed
    dependsOn: ['2']
    milestones:
      - id: M3.1
        title: Compiler operation model (IEP-0009) + operation schema
        status: completed
        evidence:
          - docs/milestones/M3.1-compiler-operation-model.md
          - spec/schema/compiler-operations-v1.schema.json
          - packages/intent-compiler/src/gate.ts
          - packages/intent-compiler/test/op-conformance.test.ts
      - id: M3.2
        title: Intent extraction + clarification engine
        status: completed
        evidence:
          - docs/milestones/M3.2-M3.4-authoring-engine.md
          - packages/intent-compiler/src/extract-rules.ts
          - packages/intent-compiler/src/compile.ts
          - packages/intent-compiler/src/clarify.ts
      - id: M3.3
        title: Confidence/provenance model + semantic diff
        status: completed
        evidence:
          - docs/milestones/M3.2-M3.4-authoring-engine.md
          - packages/intent-compiler/src/recommend.ts
          - packages/intent-compiler/src/explain.ts
      - id: M3.4
        title: Model-provider abstraction (adapters, prompt registry, limits, redaction)
        status: completed
        evidence:
          - docs/milestones/M3.2-M3.4-authoring-engine.md
          - packages/intent-compiler/src/adapter.ts
          - packages/intent-compiler/src/prompts.ts
          - packages/intent-compiler/test/authoring-e2e.test.ts
      - id: M3.5
        title: Evaluation benchmark + natural-language authoring prototype
        status: completed
        note: >-
          runAuthoringSession orchestrator (extract→compile→clarify→answer→
          confirm→apply; closed outcome taxonomy, clock-free, destructive gate
          respected) + the tools/authoring-prototype demo, and the §3.7
          evaluation benchmark (tests/evaluation, `pnpm run test:eval`, wired
          into verify): 16 cases across all eleven requirement categories
          scored on all eight measures against an implementation-independent
          oracle — 100% F1 on extraction/relationship/clarification/unsupported,
          zero false assumptions, validity+equivalence+determinism per committed
          case (123/123). Unblocks M5.3 and Phase 12.
        evidence:
          - docs/milestones/M3.5-evaluation-authoring.md
          - docs/reports/phase-3-completion.md
          - packages/intent-compiler/src/author.ts
          - packages/intent-compiler/test/author.test.ts
          - tests/evaluation/run.mjs
          - tests/evaluation/dataset.mjs
          - tools/authoring-prototype/author.mjs
    exitCriteria:
      - Natural-language requests generate valid IIS for all official examples
      - No unvalidated LLM-generated YAML is written to disk
      - Low-confidence fields require confirmation
      - Same confirmed operations produce same IIS regardless of model
      - Compiler cannot call deployment APIs
      - Every generated field has provenance

  - id: '4'
    title: Infrastructure Language Server
    priority: P1
    status: completed
    dependsOn: ['2']
    milestones:
      - id: M4.1
        title: LSP server core (diagnostics, completion, hover from schema annotations)
        status: completed
        evidence:
          - docs/milestones/M4-language-server.md
          - packages/language-server/src/providers.ts
          - packages/language-server/src/server.ts
          - packages/language-server/test/providers.test.ts
      - id: M4.2
        title: Navigation, rename, code actions, formatting
        # Definition/references/rename (all reference sites incl. profile
        # overrides; round-trip re-validates clean) + policy-autofix and
        # missing-required-field quick fixes. Document formatting is covered
        # by `iis fmt`; an LSP formatting provider is deferred with
        # formatting-preserving edits (autofix edits lose comments — documented).
        status: completed
        evidence:
          - docs/milestones/M4-language-server.md
          - packages/language-server/src/providers.ts
          - packages/language-server/test/providers.test.ts
      - id: M4.3
        title: Custom protocol extensions (canonical/architecture preview, plan preview)
        note: >-
          iis/preview (all five ch. 18 views → Mermaid) and iis/canonical
          (canonical JSON + hash, `iis normalize` parity) shipped; PLAN
          PREVIEW IS DEFERRED TO PHASE 7 — no plan format exists yet
          (IEP-0011); the iis/ request namespace is reserved for it
        status: completed
        evidence:
          - docs/milestones/M4-language-server.md
          - packages/language-server/src/server.ts
          - docs/reports/phase-4-completion.md
      - id: M4.4
        title: Performance benchmarks + caching model
        note: >-
          measured (5 cold runs each): enterprise-pci avg 16.6 ms, synthetic
          110-resource document avg 31.0 ms vs the ch. 23 §23.4 target of
          200 ms under 100 resources; caching = single-entry memoized SDK
          workspace per document version (SDK methods themselves memoized);
          150 ms debounce with stale-version publishes dropped
        status: completed
        evidence:
          - docs/milestones/M4-language-server.md
          - docs/reports/phase-4-completion.md
          - packages/language-server/test/providers.test.ts
    exitCriteria:
      - Authoring works in a generic LSP client
      - Renames safely update references
      - Diagnostics match CLI and SDK
      - Large documents remain responsive within documented limits
      - No cloud credentials required for baseline editing

  - id: '5'
    title: Reference CLI
    priority: P1
    status: completed
    dependsOn: ['2']
    milestones:
      - id: M5.1
        title: CLI skeleton + validate/format/normalize/explain/diff/graph
        status: completed
        evidence:
          - docs/milestones/M5-cli.md
          - packages/cli/src/cli.ts
          - packages/cli/test/cli.test.ts
      - id: M5.2
        title: Analysis commands (policy, cost, security, compliance, diagram)
        status: completed
        note: >-
          policy + diagram delivered over the shipped Phase 8/9 engines;
          cost/security/compliance are registered as stable stubs (exit 2,
          phase-gating message) that flip on with the Phase 10/11 engines
        evidence:
          - docs/milestones/M5-cli.md
          - packages/cli/src/commands/policy.ts
          - packages/cli/src/commands/diagram.ts
          - packages/cli/src/commands/stubs.ts
      - id: M5.3
        title: iis create (natural-language flow, gated on Phase 3)
        status: completed
        note: >-
          `iis create` wraps the Phase 3 `runAuthoringSession` engine:
          request via positional/--request/stdin, --answers/--yes-defaults/
          --acknowledge-destructive answer plumbing, human + JSON (formatVersion 1)
          output with the semantic preview, writes infrastructure.iis.yaml
          (overwrite-guarded, --stdout to skip disk), normative exit codes
          (0 committed · 1 needs-input/unsupported/validation-refused · 2 usage ·
          3 adapter failure). The created document re-validates green end to end.
          `iis edit` (incremental authoring) reuses the same engine and stays a
          phase-3 stub as the next CLI step.
        evidence:
          - docs/milestones/M5.3-iis-create.md
          - docs/reports/phase-5-completion.md
          - packages/cli/src/commands/create.ts
          - packages/cli/test/create.test.ts
      - id: M5.4
        title: Automation contracts (JSON I/O, exit codes, CI mode, plan artifacts)
        status: completed
        note: >-
          JSON I/O with formatVersion on every payload, SARIF 2.1.0, normative
          exit codes, quiet/no-color CI mode delivered; plan artifacts arrive
          with the Phase 7 planner
        evidence:
          - docs/milestones/M5-cli.md
          - docs/reports/phase-5-completion.md
          - packages/cli/src/sarif.ts
          - packages/cli/registry/error-codes.yaml
    exitCriteria:
      - CLI analysis requires no UI
      - All SDK diagnostics reproducible through the CLI
      - Output formats versioned
      - Natural-language creation produces validated IIS
      - CLI never deploys before explicit approval

  - id: '6'
    title: Provider Mapping and Plugin Framework
    priority: P1
    status: completed
    dependsOn: ['2']
    milestones:
      - id: M6.1
        title: Provider SDK + plugin manifest + loader + signature verification
        status: completed
        evidence:
          - docs/milestones/M6.1-provider-sdk.md
          - packages/provider-sdk/src/index.ts
          - packages/provider-sdk/test/loader.test.ts
          - spec/schema/plugin-manifest-v1.schema.json
          - spec/schema/conformance-case-v1.schema.json
      - id: M6.2
        title: Mock provider (full lifecycle, no external infrastructure)
        status: completed
        evidence:
          - docs/milestones/M6.2-M6.4-mock-provider-conformance.md
          - providers/mock/src/substrate.ts
          - providers/mock/src/handlers.ts
          - providers/mock/test/lifecycle.test.ts
      - id: M6.3
        title: AWS reference mappings (first production slice kinds)
        status: completed
        evidence:
          - docs/milestones/M6.3-aws-provider.md
          - providers/aws/mappings/core.iis-map.yaml
          - providers/aws/test/webapp-acceptance.test.ts
      - id: M6.4
        title: Provider conformance suite (IEP-0012)
        status: completed
        evidence:
          - docs/milestones/M6.2-M6.4-mock-provider-conformance.md
          - tests/conformance/providers.mjs
      - id: M6.5
        title: Kubernetes mapping
        status: completed
        evidence:
          - docs/milestones/M6.5-kubernetes-provider.md
          - providers/kubernetes/mappings/core.iis-map.yaml
          - providers/kubernetes/test/webapp-acceptance.test.ts
    exitCriteria:
      - Same IIS model maps to at least two targets
      - Unsupported intent fails explicitly
      - Every provider parameter is traceable
      - Provider packages cannot modify the original IIS model
      - Plugin compatibility and signature checks enforced

  - id: '7'
    title: Deterministic Planner
    priority: P1
    status: completed
    dependsOn: ['6']
    milestones:
      - id: M7.1
        title: Plan schema + planner API (IEP-0011)
        status: completed
        evidence:
          - docs/milestones/M7.1-M7.2-planner-core.md
          - spec/schema/plan-v1.schema.json
          - packages/planner/src/inputs.ts
          - packages/planner/src/plan.ts
      - id: M7.2
        title: Diff engine + lifecycle actions + graph scheduler
        status: completed
        evidence:
          - docs/milestones/M7.1-M7.2-planner-core.md
          - packages/planner/src/diff.ts
          - packages/planner/src/lifecycle.ts
          - packages/planner/src/scheduler.ts
          - packages/planner/test/plan.test.ts
      - id: M7.3
        title: Risk scoring, rollback/verification generation, plan signing/expiry
        status: completed
        evidence:
          - docs/milestones/M7.3-M7.4-planner-completion.md
          - packages/planner/src/risk.ts
          - packages/planner/src/envelope.ts
          - packages/planner/test/envelope.test.ts
      - id: M7.4
        title: Golden-plan test suite
        status: completed
        evidence:
          - docs/milestones/M7.3-M7.4-planner-completion.md
          - tests/determinism/run.mjs
          - tests/determinism/golden-plans
          - providers/mock/mappings/core.iis-map.yaml
          - packages/cli/src/commands/plan.ts
    exitCriteria:
      - Identical inputs produce byte-equivalent normalized plans
      - Plans independent of input YAML ordering
      - Destructive changes clearly marked
      - Unsupported rollback explicitly reported
      - Plan execution cannot proceed after relevant inputs change

  - id: '8'
    title: Architecture and Visualization Engine
    priority: P1
    status: completed
    dependsOn: ['2']
    milestones:
      - id: M8.1
        title: Graph derivation engine (stable node/edge identity)
        status: completed
        evidence:
          - docs/milestones/M8.1-architecture-engine.md
          - packages/architecture/src/index.ts
          - packages/architecture/test/architecture.test.ts
      - id: M8.2
        title: Exporters (JSON graph, Mermaid, Graphviz, SVG/PNG)
        status: completed
        note: >-
          JSON graph + Mermaid + DOT source shipped; SVG/PNG rasterization is a
          renderer concern per ch. 18 §18.3 (the conformance artifact is the
          textual source)
        evidence:
          - packages/architecture/src/index.ts
          - docs/reports/phase-8-completion.md
      - id: M8.3
        title: Views (intent, provider, network, security, data-flow, HA/DR, change/drift overlays)
        status: completed
        note: >-
          five ch. 18 views + diffViews change overlay shipped; provider/data-flow/HA-DR
          views await Phase 6/7 inputs; drift overlay deferred to Phase 14 with the
          diff mechanism ready
        evidence:
          - packages/architecture/src/index.ts
          - packages/architecture/test/architecture.test.ts
          - docs/reports/phase-8-completion.md
      - id: M8.4
        title: CLI + LSP integration
        status: completed
        evidence:
          - packages/cli/src/commands/diagram.ts
          - packages/language-server/src/providers.ts
    exitCriteria:
      - Every official example produces valid diagrams
      - Diagram semantics match planner dependencies
      - A plan can be visualized as an architecture change
      - Layout changes do not affect plan hashes

  - id: '9'
    title: Policy Engine
    priority: P1
    status: completed
    dependsOn: ['2']
    milestones:
      - id: M9.1
        title: Policy evaluator (deterministic, scoped)
        status: completed
        evidence:
          - docs/milestones/M9.1-policy-engine.md
          - packages/policy/src/index.ts
          - packages/policy/test/policy.test.ts
          - tests/conformance/run.mjs
      - id: M9.2
        title: Policy pack format + registry + built-in packs
        # Pack format = ch. 7 policy arrays; registry = the in-process
        # POLICY_PACKS export (six built-in packs). Organization-level pack
        # distribution/central registries are deferred to Phase 16 (M16.3).
        status: completed
        evidence:
          - docs/milestones/M9.1-policy-engine.md
          - packages/policy/src/packs.ts
          - packages/policy/test/policy.test.ts
      - id: M9.3
        title: Exception workflow model
        status: completed
        evidence:
          - docs/milestones/M9.1-policy-engine.md
          - docs/reports/phase-9-completion.md
          - packages/policy/src/index.ts
    exitCriteria:
      - Policies run before provider mapping and before deployment
      - Policy evaluation is deterministic
      - Exceptions visible in plans
      - Provider extensions cannot bypass core policy

  - id: '10'
    title: Cost Engine
    priority: P1
    status: completed
    dependsOn: ['6']
    milestones:
      - id: M10.1
        title: Cost model API + pricing snapshot format
        status: completed
        note: >-
          @iis/cost — cost is an annotation layer, never document content (ch. 16,
          IEP-0005). estimateCost(model, {costModel, snapshot}) is a pure function
          of three versioned inputs → cost-report/v1: per-resource
          hourly/monthly with exact|estimate|unknown confidence (unknown carries
          no numbers; a snapshot-missing SKU degrades visibly), application/label
          roll-ups with weakest-confidence lower-bound flagging, and deterministic
          excess-availability/orphaned suggestions. price-snapshot-v1 (content-
          addressed id#sha256) + cost-report-v1 companion schemas (harness-
          compiled, drift-tested); reference cost model + snapshot price the
          official examples byte-reproducibly.
        evidence:
          - docs/milestones/M10.1-cost-model-snapshot.md
          - spec/schema/price-snapshot-v1.schema.json
          - spec/schema/cost-report-v1.schema.json
          - packages/cost/src/estimate.ts
          - packages/cost/src/model.ts
          - packages/cost/test/cost.test.ts
      - id: M10.2
        title: Provider cost adapters + report + budget policies
        status: completed
        note: >-
          evaluateBudgets → IIS505 at plan time (deny/warn greater-than over
          x-iis-cost.*; unknown = unevaluable warning; Application budgets over
          the roll-up); mockCostModel (provider-distributed cost model — same doc
          costs differently per provider, ch. 16 §16.1); annotateModel +
          diffReports (cost delta); and `iis cost` (flips the Phase-10 stub —
          per-resource table + roll-ups + suggestions + budgets, human/JSON,
          --snapshot/--against, exit 1 on exceeded deny budget).
        evidence:
          - docs/milestones/M10.2-budget-provider-cost.md
          - docs/reports/phase-10-completion.md
          - packages/cost/src/budget.ts
          - packages/cli/src/commands/cost.ts
          - providers/mock/src/cost.ts
          - packages/cost/test/budget.test.ts
          - packages/cli/test/cost.test.ts
    exitCriteria:
      - Supported resources expose cost metadata
      - Missing price data is visible
      - Estimates reproducible from stored pricing snapshots
      - Budget violations can block planning or deployment per policy

  - id: '11'
    title: Security and Compliance Engines
    priority: P1
    status: completed
    dependsOn: ['2']
    milestones:
      - id: M11.1
        title: Security rule engine + derived least-privilege reports
        status: completed
        note: >-
          @iis/security — security is derived from the canonical document, never
          annotated (ch. 15). deriveGrants (least privilege from edge access
          only; no edge → no grant), deriveReachability (zero-trust), encryption
          posture, and IIS601-603 findings (IIS604 is plan-time) with
          deterministic risk scoring; securityReport assembles the §15.9 review
          surface. `iis security` flips the stub (human/JSON, exit 1 on error
          findings).
        evidence:
          - docs/milestones/M11.1-security-engine.md
          - packages/security/src/derive.ts
          - packages/security/src/findings.ts
          - packages/security/src/report.ts
          - packages/cli/src/commands/security.ts
          - packages/security/test/security.test.ts
      - id: M11.2
        title: Compliance bundle format + six framework bundles + evidence model
        status: completed
        note: >-
          @iis/compliance — FRAMEWORK_BUNDLES (six versioned bundles: soc2,
          pci-dss-4.0, hipaa, iso27001-2022, nist-800-53-r5, cis-8.0) with
          field + derived control rules; evaluateCompliance produces the
          evidence report (satisfied/violated/not-applicable per control with
          contributing paths, remediation, external evidence) + IIS701 control
          findings + IIS702 structural findings; label scoping (§17.7); a
          normative not-certification disclaimer. `iis compliance` flips the
          last Phase-11 stub (human/JSON, exit 1 on error findings).
        evidence:
          - docs/milestones/M11.2-compliance-engine.md
          - docs/reports/phase-11-completion.md
          - packages/compliance/src/bundles.ts
          - packages/compliance/src/evaluate.ts
          - packages/cli/src/commands/compliance.ts
          - packages/compliance/test/compliance.test.ts
    exitCriteria:
      - Every official example produces a security report
      - Compliance output distinguishes configuration coverage from certification
      - Findings contain resource references and remediation
      - Critical findings can block deployment through policy

  - id: '12'
    title: MCP and Authoritative Knowledge Framework
    priority: P1
    status: completed
    dependsOn: ['3']
    milestones:
      - id: M12.1
        title: MCP client framework + source registry + trust classification
        status: completed
        note: >-
          @iis/mcp — knowledge in, nothing out (ch. 20). KnowledgeSource (read-
          only, network-free fixtures; null = unavailable), closed
          KNOWLEDGE_CATEGORIES + TRUST_TIERS, SourceRegistry (dedup, by-category),
          and KnowledgeClient (pinned retrieval, explicit refresh as the only
          live-call point, graceful degradation — never throws on unavailability).
        evidence:
          - docs/milestones/M12-mcp-knowledge.md
          - packages/mcp/src/source.ts
          - packages/mcp/src/client.ts
          - packages/mcp/test/mcp.test.ts
      - id: M12.2
        title: Snapshot/citation/staleness model + authoring integration
        status: completed
        note: >-
          KnowledgeSnapshot (content-addressed citation model storing source,
          retrieval time, version, query, excerpt, confidence, expiry, accepted);
          createSnapshot/citation/isStale; groundRecommendation builds an
          intent-compiler mcp-origin Recommendation grounded in snapshot ids —
          accepted only through the existing TB-3 gate (IEP-0013), becoming
          explicit intent before planning (§20.3 plan invariance).
        evidence:
          - docs/milestones/M12-mcp-knowledge.md
          - docs/reports/phase-12-completion.md
          - packages/mcp/src/snapshot.ts
          - packages/mcp/src/recommend.ts
    exitCriteria:
      - MCP outages do not affect execution of finalized plans
      - Recommendations are source-attributed
      - MCP content cannot bypass validation
      - Accepted recommendations become explicit model changes

  - id: '13'
    title: IDE Integrations
    priority: P3
    status: completed
    dependsOn: ['4', '5']
    milestones:
      - id: M13.1
        title: VS Code extension
        status: completed
        note: >-
          Thin client over the Phase-4 language server (diagnostics/completion/
          hover/nav/rename/code-actions + iis/preview + iis/canonical) plus the
          IIS MCP server for authoring/analysis; no credentials for baseline
          editing. Delivered as the integration guide + scaffold
          (docs/guides/ide-integration.md); .vsix packaging is a release step
          over the tested language-server core.
        evidence:
          - docs/guides/ide-integration.md
          - docs/reports/phase-13-completion.md
      - id: M13.2
        title: IIS MCP server (authoring tools separated from deployment tools)
        status: completed
        note: >-
          @iis/mcp-server — protocol-neutral dispatcher over the read-only
          IIS_TOOLS registry (iis_author through the intent-compiler gate;
          iis_validate/cost/security/compliance over the reference engines). NO
          deployment/mutation/provider-API tool exists; assertReadOnly fails
          closed at construction (ch. 19). Every committed authoring result
          carries per-field provenance. Fully tested in-process.
        evidence:
          - docs/milestones/M12-mcp-knowledge.md
          - packages/mcp-server/src/tools.ts
          - packages/mcp-server/src/server.ts
          - packages/mcp-server/test/server.test.ts
      - id: M13.3
        title: Cursor/Claude Code/Windsurf integration guides + JetBrains prototype
        status: completed
        note: >-
          Integration guide covering Cursor/Claude Code/Windsurf (via the IIS
          MCP server) and JetBrains (via the shared LSP); all reuse the same
          engines as the CLI and cannot deploy (ch. 19).
        evidence:
          - docs/guides/ide-integration.md
    exitCriteria:
      - Users can generate and review IIS without writing YAML
      - IDE actions use the same SDK and planner as the CLI
      - Assistant tools cannot directly call provider APIs
      - Every generated value and its provenance is inspectable

  - id: '14'
    title: Deployment, State, Verification and Drift
    priority: P2
    status: completed # M14.4 AWS pilot idempotence is a documented follow-on (no cloud in verify); reference scope met
    dependsOn: ['7']
    milestones:
      - id: M14.1
        title: State backend API + local/remote implementations + locking (IEP-0010)
        status: completed
        note: >-
          @iis/state — pluggable StateBackend (read/CAS-write/appendHistory/lock
          lifecycle) + LocalStateBackend: lease locking fail-closed on a live
          lease (§5.5), CAS writes on a monotonic revision with integrity hash,
          append-only history (roadmap §4.4 fields). Injected clock. Remote
          object-storage backend implements the same interface (follow-on).
        evidence:
          - docs/milestones/M14-deployment-state-drift.md
          - packages/state/src/types.ts
          - packages/state/src/local.ts
          - packages/state/test/state.test.ts
      - id: M14.2
        title: Execution engine (mock provider full lifecycle)
        status: completed
        note: >-
          @iis/deploy deploy() — drives an approved plan through a provider
          DeploymentExecutor with fail-closed locking, approval gate (§19.6),
          atomic CAS state commit with partial-state recovery (§14.7), and
          history tying the deployment to an identity. Reference execution is the
          Phase-6 mock substrate; a fixtureExecutor drives orchestration in tests.
        evidence:
          - packages/deploy/src/deploy.ts
          - packages/deploy/src/executor.ts
          - packages/deploy/test/deploy.test.ts
      - id: M14.3
        title: Verification + drift engines + rollback framework
        status: completed
        note: >-
          @iis/deploy — post-deployment verification (converged/diverged in
          history); detectDrift on the IEP-0010 two-axis taxonomy (disposition
          reconcilable/conflicting/out-of-scope × severity benign..
          security-critical/unknown); rollback() re-applies a restoring plan and
          records rolled-back, reporting irreversible rollback rather than
          performing it silently (§14.6).
        evidence:
          - packages/deploy/src/deploy.ts
          - packages/deploy/test/deploy.test.ts
      - id: M14.4
        title: AWS execution prototype + failure-injection suite
        status: completed
        note: >-
          Failure-injection suite delivered (fixtureExecutor failOn/driftOn +
          the Phase-6 mock substrate injectable failures drive partial outcomes,
          drift, recovery). Live AWS execution needs credentials/network the
          deterministic verify harness excludes; it implements the same
          DeploymentExecutor interface over the AWS provider (follow-on,
          mock-first per Phase 6). Exit criteria met at reference scope.
        evidence:
          - docs/milestones/M14-deployment-state-drift.md
          - docs/reports/phase-14-completion.md
          - packages/deploy/src/executor.ts
    exitCriteria:
      - Mock-provider deployments support the full lifecycle
      - AWS pilot deployments are idempotent
      - Concurrent state mutation is prevented
      - State recoverable after controlled failures
      - Destructive actions require explicit approval
      - AI and MCP absent from execution

  - id: '15'
    title: Visual Infrastructure Designer
    priority: P3
    status: completed
    dependsOn: ['3', '7', '8']
    milestones:
      - id: M15.1
        title: Web designer shell (local-first) + diagram canvas
        status: completed
        note: >-
          @iis/designer DesignerSession — the headless canvas core: every edit
          (addResource/connect/setProperty/remove) commits through the gate, so
          the canvas is a view and the IIS document is the single source of
          truth. Browser shell + canvas rendering are a thin client over it.
        evidence:
          - docs/milestones/M15-visual-designer.md
          - packages/designer/src/session.ts
          - packages/designer/test/designer.test.ts
      - id: M15.2
        title: Basic/advanced editors + property inspector with provenance
        status: completed
        note: >-
          addResource/setProperty reach every supported parameter through the
          operation vocabulary; inspect() is the property inspector (effective
          spec + per-field provenance). Rejected edits are no-ops on the doc.
        evidence:
          - packages/designer/src/session.ts
          - packages/designer/test/designer.test.ts
      - id: M15.3
        title: Plan approval experience + git diff support
        status: completed
        note: >-
          The session output is an ordinary IIS document, so plan approval reuses
          `iis plan` (Phase 7) and git diff is the document diff — no proprietary
          project format. UI surfaces are thin clients (docs).
        evidence:
          - docs/milestones/M15-visual-designer.md
    exitCriteria:
      - Non-experts can create the official example workloads without YAML
      - Advanced users can configure all supported provider parameters
      - UI changes produce valid, deterministic IIS
      - The UI never becomes a second source of truth

  - id: '16'
    title: Enterprise Control Plane
    priority: P3
    status: completed
    dependsOn: ['14']
    milestones:
      - id: M16.1
        title: Multi-tenant API + RBAC + approval engine
        status: completed
        note: >-
          @iis/control-plane — RBAC (viewer/author/approver/admin x actions),
          separation-of-duties approval engine (author can never approve own
          change; only approver role may; approval recorded as evidence),
          multi-tenant project isolation + append-only audit. HTTP API is a thin
          client.
        evidence:
          - docs/milestones/M16-control-plane.md
          - packages/control-plane/src/rbac.ts
          - packages/control-plane/src/project.ts
          - packages/control-plane/test/control-plane.test.ts
      - id: M16.2
        title: Git application + PR checks (intent/cost/security/compliance deltas)
        status: completed
        note: >-
          prChecks(base, head) — intent (added/removed/changed), cost (monthly
          delta + budget breaches), security (risk change + new findings),
          compliance (new violations), each pass/fail with an overall gate that
          blocks a regressing PR. Reuses the reference engines. Git app is a
          thin client (webhooks/status).
        evidence:
          - packages/control-plane/src/checks.ts
          - packages/control-plane/test/control-plane.test.ts
      - id: M16.3
        title: Policy/profile distribution + registries + audit pipeline + state service
        status: completed
        note: >-
          Project inherits org-distributed policyPacks (central enforcement
          seam); append-only audit log; the state service is @iis/state
          (Phase 14) behind control-plane authorization. Central registries and
          DR/replication are operational follow-ons over the same core.
        evidence:
          - packages/control-plane/src/project.ts
          - docs/milestones/M16-control-plane.md
    exitCriteria:
      - Multiple teams safely manage isolated projects
      - Separation of duties is enforceable
      - Every deployment tied to identity and approval evidence
      - Organization policies centrally enforced

  - id: '17'
    title: Advanced AI Assistance
    priority: P3
    status: completed
    dependsOn: ['3', '12']
    milestones:
      - id: M17.1
        title: AI review engine + explainability reports
        status: completed
        note: >-
          @iis/ai-review reviewDocument — deterministic review lens folding the
          security + compliance engines and best-practice rules into explainable,
          source-cited ReviewItems (rule id + explanation + suggestion). Pure,
          never mutates.
        evidence:
          - docs/milestones/M17-advanced-ai.md
          - packages/ai-review/src/review.ts
          - packages/ai-review/test/ai-review.test.ts
      - id: M17.2
        title: Organization-context adapters + guardrails + evaluation dashboard
        status: completed
        note: >-
          assertGuardrails enforces advisory-only (never blocking, §20.2.3) +
          always-cited; acceptance rides the intent-compiler recommend->accept->
          gate (TB-3); a model change cannot alter an approved plan (plan
          envelopes bind planId). Org context = MCP enterprise snapshot ids
          threaded as citations; the deterministic review IS the eval (dashboard
          is a rendering).
        evidence:
          - packages/ai-review/src/review.ts
          - packages/ai-review/test/ai-review.test.ts
    exitCriteria:
      - AI suggestions never alter infrastructure without validation and explicit acceptance
      - Recommendations cite sources
      - Platform operates without AI
      - Model changes cannot alter an approved plan

  - id: '18'
    title: Ecosystem, Migration and Open Standardization
    priority: P4
    status: completed
    dependsOn: ['14']
    milestones:
      - id: M18.1
        title: Migration importers (Terraform, CloudFormation, Pulumi, Crossplane, K8s, live resources)
        status: completed
        note: >-
          @iis/migrate importKubernetes — translates K8s manifests into IIS
          THROUGH the operation gate (Deployment->Service, Job->Job,
          PVC->Volume, Ingress->Gateway, Secret->Secret); unmappable constructs
          (ConfigMap, k8s Service) reported explicitly, never guessed. Imported
          IIS validates green; deterministic. Other source formats implement the
          same ImportResult contract (follow-on).
        evidence:
          - docs/guides/ecosystem.md
          - packages/migrate/src/kubernetes.ts
          - packages/migrate/test/migrate.test.ts
      - id: M18.2
        title: CI/CD integrations (GitHub Actions, GitLab, Jenkins, Argo, Backstage)
        status: completed
        note: >-
          Integrations shell out to the CLI (validate/cost/security/compliance —
          exit codes gate the pipeline; SARIF feeds code-scanning); the control
          plane prChecks (Phase 16) is the hosted git-app form. GitHub Actions
          example + platform mapping in docs/guides/ecosystem.md.
        evidence:
          - docs/guides/ecosystem.md
      - id: M18.3
        title: Registries (providers, extensions, policy packs, profiles) + certification
        status: completed
        note: >-
          Versioned, signed, digest-pinned artifact index over the existing
          package/trust model (plugin.iis.dev/v1 + trust store, Phase 6);
          certification = passing the conformance program at a level
          (test:providers). Documented; hosted registry service is operational.
        evidence:
          - docs/guides/ecosystem.md
      - id: M18.4
        title: Open governance (TSC, external IEPs, LTS policy)
        status: completed
        note: >-
          Implementation-independent spec/schema/conformance; every normative
          change via an accepted IEP (Phase 0.5); TSC review, LTS policy, schema-
          compatibility contract. Documented governance model.
        evidence:
          - docs/guides/ecosystem.md
          - docs/reports/phase-18-completion.md
    exitCriteria:
      - At least two independent implementations pass core conformance
      - Multiple provider packages pass provider conformance
      - Ecosystem not dependent on one UI, one LLM, or one cloud
      - Specification and conformance suite remain implementation-independent
