{
  "name": "@iap/control-plane",
  "version": "0.1.0",
  "description": "Enterprise control-plane core for IaP (roadmap Phase 16): multi-tenant project isolation, RBAC, a separation-of-duties approval engine tying every deployment to identity and approval evidence, an append-only audit log, and PR checks that report intent/cost/security/compliance deltas between a base and head document.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "license": "SEE LICENSE (TBD)",
  "dependencies": {
    "@iap/compliance": "workspace:*",
    "@iap/cost": "workspace:*",
    "@iap/model": "workspace:*",
    "@iap/security": "workspace:*"
  },
  "devDependencies": {
    "@iap/sdk": "workspace:*",
    "@types/node": "^22.20.1",
    "typescript": "^5.7.0"
  }
}
