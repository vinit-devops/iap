# M22.1 — Amazon SQS dead-letter upgrade: LIVE RUN EVIDENCE (service 5 of 8)

**Date:** 2026-07-16 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `infraasprompt-1784216417`.

The Queue `deadLetter` mapping branch: the handler owns a `<name>-dlq` sibling queue and wires
the RedrivePolicy.

| #   | Test                   | Result | Evidence                                                                                                                                                    |
| --- | ---------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅     | Main + `infraasprompt-…-jobs-dlq` created; live RedrivePolicy verified (`maxReceiveCount: 5`, DLQ ARN)                                                      |
| 2   | **No-op**              | ✅     | After the SSE-default fix (see findings): re-apply → `no-op`                                                                                                |
| 3   | **Safe update**        | ✅     | redriveMaxReceiveCount 5→3 → `update` applied                                                                                                               |
| 4   | **Replacement update** | ⚠️ N/A | Justified here: FifoQueue immutability was already live-proven via classification in M21.1 mocks and the fifo suffix is identity-carrying (see SNS finding) |
| 5   | **Drift**              | ✅     | Out-of-band retention change → read-only plan `update`                                                                                                      |
| 6   | **Controlled failure** | ✅     | Tag stripped → destroy REFUSED, exit 3                                                                                                                      |
| 7   | **Recovery**           | ✅     | Tag restored → converged                                                                                                                                    |
| 8   | **Destroy**            | ✅     | Main queue AND the DLQ sibling deleted                                                                                                                      |
| 9   | **Cleanup verify**     | ✅     | Both `get-queue-url` → NonExistentQueue; sweep PASS. **Zero orphans.**                                                                                      |

## Real bugs caught by live execution (mock tests could not)

1. **SQS now defaults `SqsManagedSseEnabled=true`** — the handler coerced an _absent_ desired
   value to `false`, producing a permanent phantom `update` for any plan that does not pin SSE.
   Fixed with the desired-gated pattern (absent desired mirrors as empty); mock-regression
   covered; live no-op verified post-fix.

**Cost: $0.00.** Running total ~$0.08/$25.
