# Phase 2 Completion Report — Canonical Model and Reference SDK

**Date:** 2026-07-10 · **Milestones:** M2.1–M2.6 (all completed with reviewable docs under `docs/milestones/`)

## Exit criteria verification

| Exit criterion                                                        | Status   | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Every official example parses and validates                           | **Pass** | All 9 examples run through `validateDocument` phases 1–4 with zero errors (`packages/validator/test/validator.test.ts`, `examples/*` block, 9 tests) and again through the SDK facade end to end (`packages/sdk/test/sdk.test.ts`, one `load → validate → canonical → graph → waves → round-trip` test per example, 9 tests). The harness (`pnpm run test:spec`, 57 checks) schema-validates the same corpus on every run.                                                                                                                   |
| Parse → canonicalize → serialize → parse produces an equivalent model | **Pass** | Round-trip test inside each of the 9 per-example SDK tests: `load(ws.serialize('yaml')).canonical().hash === ws.canonical().hash`; also holds for an invalid document (`unknown kind … canonical() still works` test) and relative to a profile (`round-trips relative to a profile`). Canonicalization is additionally idempotent — `canonicalize(canonicalJson)` reproduces the same hash (`packages/model/test/canonicalize.test.ts`, `idempotence` block).                                                                               |
| Identical inputs generate identical canonical hashes                  | **Pass** | Golden vectors with pinned SHA-256 hashes (`canonicalize.test.ts`: `golden-queue: pinned canonical JSON and hash`, `golden-web-db: pinned hash`, `golden-profiled (profile production): pinned hash`); `documents differing only in key/resource order hash identically`; `default invisibility: explicit defaults and omissions hash identically`; string-vs-file inputs hash identically through the SDK (`sdk.test.ts`). No floating point anywhere in canonicalization (BigInt milli-units, `packages/model/src/quantity.ts`, 44 tests). |
| Graph results independent of input key ordering                       | **Pass** | `packages/graph/test/graph.test.ts` → `executionWaves … is independent of resource-map insertion order`; edge normalization sorts matches and edges deterministically regardless of authoring order (`canonicalize.test.ts`, `key-order independence` and `relationship flattening` blocks); the SDK builds its graph over the canonical model, inheriting order independence by construction.                                                                                                                                               |
| No provider SDK required to parse core IaP                            | **Pass** | Runtime dependency audit (`pnpm ls --prod` per package): `@iap/model` — none; `@iap/graph` — `@iap/model`; `@iap/parser` — `@iap/model` + `ajv@8` + `yaml@2`; `@iap/validator` — workspace packages only; `@iap/sdk` — workspace packages + `yaml@2`. Full transitive closure: ajv's four utility deps (`fast-deep-equal`, `fast-uri`, `json-schema-traverse`, `require-from-string`). No cloud/provider SDK anywhere; the IAP105 banned-term scan keeps provider nouns out of core positions.                                               |
| Public APIs have compatibility tests                                  | **Pass** | `packages/sdk/test/api-surface.test.ts` (5 tests) pins the exact module export names, the `IaPWorkspaceResult` property surface with and without `sourceMap`, and the exact method set — removals or renames fail the suite. Beneath the facade, `packages/model/test/model.test.ts` drift tests pin constants ≡ schema enums and embedded ≡ spec schemas.                                                                                                                                                                                   |

## Deliverables checklist (phase-2 design)

`@iap/parser` extended ✓ (M2.1: per-node source maps, alias limits, file/stream input, position-attached findings) · `@iap/model` extended ✓ (M2.2+M2.3: CIM types per IEP-0008, exact-rational quantity engine, RFC 7386 profile merger, schema-driven default materializer, §4.7 edge flattener, canonical serializer + SHA-256, golden vectors) · `@iap/graph` ✓ (M2.5: indexes, constraint tables, ordering DAG, Tarjan cycle detection, execution waves, impact queries) · `@iap/validator` ✓ (M2.4: phases 1–4, registry-coded findings, post-merge re-validation) · `@iap/sdk` ✓ (M2.6: `load` facade, round-trip serializer, extension registration, API surface tests) · Harness upgrade ✓ (semantic conformance cases executed against the real validator — Phase 1's "declared outcome" checks are now real semantic verification; 16 counted semantic checks, 5 explicitly deferred to phase-5+ engines).

## Verification state

`pnpm run verify` green end to end: build ×5 packages, ESLint clean, **260 passed + 5 skipped** unit tests across 9 files (44 quantity, 11 model, 56 canonicalize, 43 parser, 15 source-map, 23 graph, 44 validator, 24 sdk, 5 api-surface), **57/57** harness checks. `pnpm run format:check` clean.

## Remaining scope notes

- Validation phases 5–8 (policy, security, compliance, version/extension) belong to Phases 9/11 engines; their five conformance cases stay explicitly deferred in the harness and skipped (with reason) in the validator suite.
- One registry gap recorded in the M2.6 milestone report: no dedicated code for registered-but-schema-invalid extension content — emitted as IAP802/error pending a future IEP (candidate IAP806).
- Golden canonical hashes are single-implementation pins for now; they become cross-implementation determinism vectors when a second implementation exists (ch. 24 CP class, Phase 18).

## Decision

Phase 2 is **complete**. Phases gated only on Phase 2 are unblocked: Phase 3 (intent compiler), Phase 4 (LSP), Phase 5 (CLI), Phase 6 (provider mapping), Phase 8 (visualization), Phase 9 (policy), Phase 11 (security/compliance).
