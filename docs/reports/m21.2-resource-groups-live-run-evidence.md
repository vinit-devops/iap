# M21.2 — AWS Resource Groups: LIVE RUN EVIDENCE (service 1 of 4)

**Date:** 2026-07-16 · **Status:** COMPLETE — executed against **real AWS** and fully torn down.
**Account:** REDACTED · **Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess`
(SSO AdministratorAccess) · **Run id:** `iapg-1784203769`.

This is genuine live evidence, not a mock. The IaP CLI (`iap deploy`/`destroy`/`drift`) drove
the real `@aws-sdk` executor through the new `aws:resourcegroups:Group` handler (M21.2) —
create, reconcile, drift, fail-closed refusal, recovery, and delete against live AWS. First
per-service run under the individual-run verification bar (requester-directed 2026-07-16).

## Workload

One IaP document: `Application` (subject under test → `aws:resourcegroups:Group`) grouping one
`ObjectStore` member (companion → `aws:s3:Bucket`, handler already live-proven in M19.3; the
schema requires an Application to group ≥1 real resource). Applied with `--confirm`, all
resources tagged `iap:managed=true` / `iap:planId` / `iap:resourceId`; group version rides as
`iap:applicationVersion`.

## The 9 lifecycle tests

| #   | Test                   | Result   | Evidence                                                                                                                                                      |
| --- | ---------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅       | Live ARNs: `arn:aws:resource-groups:eu-central-1:REDACTED:group/iapg-1784203769-app`, `arn:aws:s3:::iapg-1784203769-store`; exit 0, state revision 1          |
| 2   | **No-op**              | ✅       | Immediate re-run → both `no-op` ("present and converged"), 0 applied; idempotent on first try                                                                  |
| 3   | **Safe update**        | ✅       | `spec.version` 1.0.0→1.1.0 → group `update-in-place`; live `iap:applicationVersion` tag verified `1.1.0` by direct `get-tags`                                  |
| 4   | **Replacement update** | ⚠️ N/A   | Justified: the handler declares no immutable projection keys — a group's name IS its identity and query type reconciles via UpdateGroupQuery. First live replacement lands at DynamoDB (M22.2). |
| 5   | **Drift**              | ✅       | Out-of-band `resource-groups tag` set version tag to 9.9.9 → `iap drift` reports 1 diverged (`update`), read-only, exit 0                                     |
| 6   | **Controlled failure** | ✅       | Out-of-band strip of `iap:managed` → `destroy --confirm` REFUSED the group ("managed-only destroy"), error in `errors[]`, exit 3, **no silent success**; group untouched |
| 7   | **Recovery**           | ✅       | Managed tag restored → re-deploy converged: group reconciled (9.9.9 drift healed), member bucket recreated, `0 failed`, exit 0                                 |
| 8   | **Destroy**            | ✅       | `iap destroy --confirm` → both DELETED, exit 0, managed-only                                                                                                  |
| 9   | **Cleanup verify**     | ✅       | `get-group` → NotFoundException; `head-bucket` → 404; `iap drift` → both `absent`; harness sweep (`live:sweep`) → zero tagged orphans, zero `iapg-*` roles. **Zero orphans.** |

## Real bugs caught by live execution (mock tests could not)

None in the handler. The Resource Groups handler passed all lifecycle stages on first live
contact — including projection round-trip idempotence (test 2), the class of bug M19.3 caught
twice in SQS. One harness-usage note (not a product defect): piping CLI output through `tail`
masks the exit code; evidence runs must capture `$?` unpiped (done from test 6 onward, and the
test-6 refusal was re-verified unpiped → exit 3).

## Honest scope notes

- Subject under test was **Resource Groups only**; the S3 member exercised an already-proven
  handler. Secrets Manager, ACM, and TargetGroup are separate upcoming per-service runs.
- Pre-flight created the account's `iap-live-runs` budget ($25 monthly, 80% email alert) —
  it did not exist; pre-flight correctly failed closed until it did.
- Credentials were broad SSO AdministratorAccess (accepted for this near-zero-cost run; a
  least-privilege role remains recommended for routine use).
- The group's tag-filter query (`iap:application=<name>`) is asserted structurally, not
  exercised against member enumeration — group membership semantics are M21.3+ concerns.

## Cost

Resource group (free) + empty S3 bucket, live for ~10 minutes, then destroyed; AWS Budgets
(first budgets are free). **This run: $0.00.** **Roadmap running total: $0.00 of the $25.00
ceiling.** (Figures are resource-hour estimates; Cost Explorer actuals lag ~24h and are
reconciled at M24.4 closeout.)
