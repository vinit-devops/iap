# M22.3 — AWS Aurora Serverless v2 (relational Database): LIVE RUN EVIDENCE (service 1 of 6)

**Date:** 2026-07-17 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `infraasprompt-1784307755`.

One Aurora Serverless v2 stack — subnet group `infraasprompt-1784307755-subnets` (default-VPC subnets,
ADR-0005) → cluster `infraasprompt-1784307755-db` (aurora-postgresql, ServerlessV2 min 0 / max 1 ACU,
StorageEncrypted, AWS-managed master secret via `ManageMasterUserPassword`, deletion protection
off) → writer instance `infraasprompt-1784307755-writer` (`db.serverless`) — wired by `dependsOn` and
driven end to end through the `AwsExecutor` with a hand-built `ProviderPlan`. This was a **clean
re-run**: a prior attempt (`infraasprompt-1784289027`) was killed by a session limit mid-test and its
resources were torn down beforehand; this run started from a verified zero-orphan slate.

## Workload

`aws:rds:DBSubnetGroup` → `aws:rds:DBCluster` (dependsOn subnet group) → `aws:rds:DBClusterInstance`
(dependsOn cluster). Applied with the executor's live gate open, all tagged `iap:managed=true` /
`iap:planId` / `iap:resourceId`. The executor orders topologically and reverses on destroy.

## The 9 lifecycle tests

| #   | Test                   | Result | Evidence |
| --- | ---------------------- | ------ | -------- |
| 1   | **Create**             | PASS   | Subnet group → cluster → instance created in topological order. ARNs: `arn:aws:rds:eu-central-1:REDACTED:subgrp:infraasprompt-1784307755-subnets`, `…:cluster:infraasprompt-1784307755-db`, `…:db:infraasprompt-1784307755-writer`. Cluster `available` ~30s; writer `available` ~7 min (creating → configuring-enhanced-monitoring → available). Live verified: Engine `aurora-postgresql` (mapped from `postgresql`), ServerlessV2 Min 0 / Max 1 ACU, BackupRetentionPeriod 1, DeletionProtection false, StorageEncrypted true. `iap:managed`/`iap:planId`/`iap:resourceId` present on all three (subnet group also carries `iap:subnetTier=data`; instance `db.serverless`, aurora-postgresql, attached to cluster). Master secret is AWS-owned `arn:aws:secretsmanager:…:secret:rds!cluster-f3ff3b53-…` (SecretStatus `active`) — no password material through IaP. |
| 2   | **No-op**              | PASS   | Immediate re-plan + re-apply (gate open) → all three `no-op` "present and converged", `errors: []` |
| 3   | **Safe update**        | PASS   | backupRetentionPeriod `1`→`2` planned `update` on the cluster (subnet group + instance `no-op`), applied via ModifyDBCluster, live-verified `2`; re-plan `no-op` |
| 4   | **Replacement update** | PASS (execution JUSTIFIED-DEFERRED) | engine `postgresql`→`mysql` (maps `aurora-mysql`) planned `replace` on the cluster ("present but immutable attribute drifted — will replace (delete+create)"). Replace gate CLOSED → REFUSED verbatim, exit 3, zero mutation (live Engine still `aurora-postgresql`): "refusing to replace infraasprompt-1784307755-db.aws:rds:DBCluster: immutable attribute drift requires delete+create (destructive); re-run with the replacement gate open". Execution of the replace deferred per M21.3 precedent — an Aurora cluster delete+create is ~20+ min wall clock; the roadmap-mandated real replacement execution was already discharged at M22.2 (DynamoDB keySchema). |
| 5   | **Drift**              | PASS   | Out-of-band `aws rds modify-db-cluster --backup-retention-period 3 --apply-immediately` → read-only plan classified `update` on the cluster; live value still `3` after the plan (read-only proven); apply reconciled → live `2`; re-plan `no-op` |
| 6   | **Controlled failure** | PASS   | `iap:managed` stripped from all three (remove-tags-from-resource) → destroy REFUSED all three, exit 3, zero deletion, reversed topology (instance → cluster → subnet group): "refusing to delete <logicalId>: not tagged iap:managed=true (managed-only destroy)" |
| 7   | **Recovery**           | PASS   | `iap:managed=true` restored on all three → non-destroy dry-run `no-op`, `errors: []`, exit 0; destroy plan classifies all three "managed — will delete" |
| 8   | **Destroy**            | PASS   | Reversed topology (instance → cluster → subnet group). Pass 1: DeleteDBInstance + DeleteDBCluster accepted (both → `deleting`); subnet-group delete rejected while the member instance still existed ("Cannot delete the subnet group 'infraasprompt-1784307755-subnets' because at least one database instance: infraasprompt-1784307755-writer is still using it"). Polled to NotFound (instance gone ~10 min, cluster gone ~12 min), re-ran destroy → instance/cluster classified `no-op` (already absent), subnet group deleted. Final destroy `errors: []`, driver exit 0. |
| 9   | **Cleanup verify**     | PASS   | describe-db-subnet-groups / describe-db-clusters / describe-db-instances → NotFound (DBSubnetGroupNotFoundFault / DBClusterNotFoundFault / DBInstanceNotFound). AWS-managed master secret `rds!cluster-f3ff3b53-…` gone (describe-secret → ResourceNotFoundException — deleted with the cluster). Run-scoped tag sweep (`iap:managed=true` filtered to `1784307755`) → `[]`. **Zero run-scoped orphans.** |

## Real bugs caught by live execution (mock tests could not)

None — the handlers behaved to spec throughout: engine mapping (`postgresql`→`aurora-postgresql`,
`mysql`→`aurora-mysql`), desired-gated engineVersion (AWS default minor never read as drift),
Serverless v2 scale-to-zero posture, AWS-managed master secret, the managed-only destroy gate,
the immutable-engine replacement gate, and the reversed-topology teardown that respects the
subnet-group-outlives-cluster ordering (an M22.2 live finding, confirmed again here).

## Honest scope notes

- **Clean re-run.** Supersedes the session-limit-interrupted `infraasprompt-1784289027` attempt, whose
  resources were torn down before this run began (verified zero orphans at start).
- **Replacement execution deferred** (test 4) — classification, gate-closed refusal, and
  zero-mutation proven live; the destructive delete+create itself was justified-deferred on
  wall-clock grounds (M21.3 precedent; M22.2 discharged the mandated execution).
- **No mapping emission** for the relational Database kind yet — per-branch outputs grammar gap
  (deferred like M22.1 apigatewayv2 / M22.2 dynamodb); this run drove the executor directly with a
  hand-built `ProviderPlan`.
- **Default VPC** (ADR-0005): the subnet group spans the account's default-VPC subnets; the M23.4
  Network handlers replace this pragmatism.
- **Single writer, no reader/failover**, no customer-KMS (StorageEncrypted uses the AWS-owned
  default key), engine minor version unpinned.
- **Parallel-wave sweep:** 7 sibling M22.3 per-service runs were live in the same account/region,
  so the region-global `live:sweep` was NOT run here — cleanup verification is run-scoped by run
  id (`1784307755`); the coordinator runs the global sweep after the wave.

## Cost

Serverless v2 at min 0 ACU (scale-to-zero, SecondsUntilAutoPause 300) — compute ≈ $0 at idle;
brief ~1 ACU baseline during the ~15 min create/exercise/destroy lifetime at ~$0.12/ACU-hr ≈
a few cents. Storage on an empty encrypted volume + backup + the AWS-managed master secret,
prorated over the run, are sub-cent. **Estimated: ~$0.05 for this service run.** Wave running
total finalized at closeout (M24.4); M22.2 baseline was ~$0.00 over its four services.
