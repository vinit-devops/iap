# M22.1 — Amazon S3 public-exposure upgrade: LIVE RUN EVIDENCE (service 4 of 8)

**Date:** 2026-07-16 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `infraasprompt-1784216247`.

The ObjectStore `exposure: public` mapping branch: PublicAccessBlock off + public-read bucket
policy, proven end to end with an anonymous HTTP read.

| #   | Test                   | Result | Evidence                                                                                                     |
| --- | ---------------------- | ------ | ------------------------------------------------------------------------------------------------------------ |
| 1   | **Create**             | ✅     | Public bucket; staged object; **anonymous `curl` returned the object body** (public read proven end to end)    |
| 2   | **No-op**              | ✅     | Re-apply → `no-op`                                                                                            |
| 3   | **Safe update**        | ✅     | Flip to private (`blockPublicAccess: true`) → `update`; anonymous read then returned **HTTP 403** (lockout proven) |
| 4   | **Replacement update** | ⚠️ N/A | Justified: no immutable projection keys on this handler                                                       |
| 5   | **Drift**              | ✅     | Out-of-band PublicAccessBlock loosened → read-only plan `update`; reconcile re-locked it                       |
| 6   | **Controlled failure** | ✅     | Destroy failed CLOSED on lingering object versions ("must delete all versions"), error recorded, exit 3       |
| 7   | **Recovery**           | ✅     | Versions purged → destroy converged                                                                            |
| 8   | **Destroy**            | ✅     | DeleteBucket, exit 0                                                                                          |
| 9   | **Cleanup verify**     | ✅     | head-bucket → 404; sweep PASS. **Zero orphans.**                                                              |

## Live findings

1. **Versioned-bucket teardown needs a version purge** — a bucket that ever had versioning
   applied (even `Suspended`) accumulates versions/delete-markers; `DeleteBucket` fails closed
   until they are purged. Runbook note added for ObjectStore teardowns; a handler-level purge
   (managed-only) is a candidate for the M24.4 hardening list.

**Cost: $0.00.** Running total ~$0.08/$25.
