# M21.3 — Amazon RDS (DBInstance + DBSubnetGroup): LIVE RUN EVIDENCE (service 4 of 4)

**Date:** 2026-07-16 · **Status:** COMPLETE — executed against **real AWS** and fully torn down.
**Account:** REDACTED · **Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess`
(SSO AdministratorAccess) · **Run id:** `infraasprompt-1784210467`.

The real `AwsExecutor` drove both RDS handlers: `aws:rds:DBSubnetGroup` (default-VPC subnets,
tier tag) and `aws:rds:DBInstance` (postgres db.t4g.micro, encrypted, deletion-protected,
RDS-managed master password — no credential material ever passed through IaP).

## Workload

Synthetic plans: `aws:rds:DBSubnetGroup` (`infraasprompt-1784210467-db-subnets`, tier `private`) and
`aws:rds:DBInstance` (`infraasprompt-1784210467-db`: postgres, db.t4g.micro, 20 GiB, encrypted at
rest, publiclyAccessible false, backupRetention 7, deletionProtection TRUE,
`ManageMasterUserPassword: true`) referencing the subnet group.

## The 9 lifecycle tests

| #   | Test                   | Result | Evidence                                                                                                                                   |
| --- | ---------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Create**             | ✅     | Subnet group ARN + instance ARN live; `rds wait db-instance-available` green (~8 min); master credentials RDS-managed                          |
| 2   | **No-op**              | ✅     | Both resources re-apply → `no-op` first try (subnet-group tier round-trips via `iap:subnetTier` tag)                                          |
| 3   | **Safe update**        | ✅     | backupRetention 7→1 → targeted `ModifyDBInstance` (only the drifted field, ApplyImmediately), exit 0                                          |
| 4   | **Replacement update** | ⚠️ partial (justified) | engine postgres→mysql → plan `replace` + gate-closed refusal exit 3 LIVE. Gated execution deferred BY ROADMAP DESIGN: the mandated first data-engine replacement exercise is DynamoDB's key schema at M22.2 |
| 5   | **Drift**              | ✅     | Out-of-band `modify-db-instance --backup-retention-period 5` → read-only plan classifies `update`                                             |
| 6   | **Controlled failure** | ✅     | Managed tag stripped → destroy REFUSED, exit 3, instance untouched                                                                            |
| 7   | **Recovery**           | ✅     | Tag restored → re-apply converged (`update` healed the retention drift, `errors: []`, exit 0)                                                 |
| 8   | **Destroy**            | ✅     | Deletion protection DISABLED first (ModifyDBInstance), then DeleteDBInstance(SkipFinalSnapshot + DeleteAutomatedBackups); `wait db-instance-deleted` green; subnet group deleted after |
| 9   | **Cleanup verify**     | ✅     | DBInstanceNotFound + DBSubnetGroupNotFoundFault on direct reads; sweep PASS. **Zero orphans.** (RDS-managed master secret is deleted with the instance.) |

## Real bugs caught by live execution (mock tests could not)

None in the handlers — including the two riskiest paths: the deletion-protection
disable-then-delete sequence and the ManageMasterUserPassword create. One wave-level process
finding: the account's `iap-live-runs` budget limit had been changed from $25 to $50 outside
this session, and pre-flight CORRECTLY failed closed on it (flagged to the approver; not
modified by the agent).

## Honest scope notes

- Executor-driven synthetic plans (the CLI mapping path for Database also emits the
  connection Secret — handler exists since M21.2; full-path exercise at M23.4's retrofit).
- `requireSecureTransport` needs a parameter group — recorded gap until parameter-group
  support lands.
- `engineVersion` compares only when pinned (unpinned minor upgrades don't read as drift).

## Cost

db.t4g.micro + 20 GiB gp2 for ~45 minutes ≈ **$0.02**. Wave total (ECS + ALB + ElastiCache +
RDS) ≈ **$0.08**. **Roadmap running total: ~$0.08 of $25.00.**
