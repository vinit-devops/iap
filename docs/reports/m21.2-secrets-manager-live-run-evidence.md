# M21.2 — AWS Secrets Manager: LIVE RUN EVIDENCE (service 2 of 4)

**Date:** 2026-07-16 · **Status:** COMPLETE — executed against **real AWS** and fully torn down.
**Account:** REDACTED · **Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess`
(SSO AdministratorAccess) · **Run id:** `infraasprompt-1784205298`.

This is genuine live evidence, not a mock. The real `AwsExecutor` drove the new
`aws:secretsmanager:Secret` handler (M21.2) through create, idempotence, reconcile, drift,
fail-closed refusal, recovery, and force-delete teardown against live AWS.

## Workload

One synthetic single-resource provider plan: `aws:secretsmanager:Secret`
(`infraasprompt-1784205298-secret`, `generateSecretString: true` → value generated via
GetRandomPassword, never logged, **never read back**), tagged `iap:managed=true` /
`iap:planId` / `iap:resourceId`. Executor-driven (see honest scope).

## The 9 lifecycle tests

| #   | Test                   | Result | Evidence                                                                                                                                         |
| --- | ---------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅     | Live ARN `arn:aws:secretsmanager:eu-central-1:REDACTED:secret:infraasprompt-1784205298-secret`; exit 0                                                  |
| 2   | **No-op**              | ✅     | Immediate re-apply → `no-op`, `applied: false`; idempotent on first try                                                                          |
| 3   | **Safe update**        | ✅     | Description change → `update`; live `Description` verified reconciled by direct `describe-secret`                                                |
| 4   | **Replacement update** | ⚠️ N/A | Justified: the handler declares no immutable projection keys — name IS identity; description reconciles in place; the value is write-only        |
| 5   | **Drift**              | ✅     | Out-of-band `update-secret --description "out-of-band drift 9.9.9"` → read-only plan classifies `update` ("present but drifted — will reconcile") |
| 6   | **Controlled failure** | ✅     | Out-of-band strip of `iap:managed` → destroy REFUSED ("managed-only destroy"), error in `errors[]`, exit 3, **no silent success**                |
| 7   | **Recovery**           | ✅     | Managed tag restored → re-apply converged (`update`, `errors: []`, exit 0)                                                                       |
| 8   | **Destroy**            | ✅     | `delete` applied with **ForceDeleteWithoutRecovery** (zero-orphan teardown), exit 0                                                              |
| 9   | **Cleanup verify**     | ✅     | Force deletion is async: seconds later `describe-secret` → ResourceNotFoundException; harness sweep → zero orphans. The handler reads a `DeletedDate` secret as absent, so classification stays correct during the async window. |

## Real bugs caught by live execution (mock tests could not)

None. One live-only behavior confirmed and worth recording: **force deletion is asynchronous**
— `DescribeSecret` can still return metadata (with `DeletedDate`) seconds after
`DeleteSecret(ForceDeleteWithoutRecovery)`. The handler's absent-on-`DeletedDate` rule (written
at mock time) is what keeps post-destroy reads honest; without it, cleanup verify would flap.

## Honest scope notes

- **Executor-driven synthetic plan**, not the CLI mapping path: the mapping realizes
  `aws:secretsmanager:Secret` only alongside `aws:rds:*` targets whose handlers land in M21.3
  — the CLI path would (correctly) fail closed today and gets exercised end-to-end at M21.3.
- The secret VALUE was generated server-side (GetRandomPassword), stored once, and never read,
  logged, or asserted on — only metadata (description, tags) is the drift surface.
- First run under the `infraasprompt-<epoch>` naming directive (2026-07-16).

## Cost

One secret, live for ~4 minutes, force-deleted (no recovery-window storage). Secrets Manager
bills $0.40/secret/month prorated — this run rounds to **$0.00** (<$0.01).
**Roadmap running total: $0.00 of the $25.00 ceiling.**
