# M23.2 — AWS Redshift Serverless (data-warehouse Database): LIVE RUN EVIDENCE (service 5 of 6)

**Date:** 2026-07-18 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `infraasprompt-1784345754`.

One Redshift Serverless warehouse — namespace `infraasprompt-1784345754-ns` (dbName `iapdb`,
adminUsername `iapadmin`, **Secrets Manager-managed admin password** via `manageAdminPassword`,
AWS-owned KMS) → workgroup `infraasprompt-1784345754-wg` (baseCapacity 8 RPU — the serverless floor,
`publiclyAccessible=false`, `enhancedVpcRouting=false`) — wired by the workgroup's `namespaceName`
cross-reference and a `dependsOn` edge, driven end to end through the `AwsExecutor` with a
hand-built `ProviderPlan`. **Zero queries were run** (Redshift Serverless bills RPU on
query-seconds), so compute cost is ≈ $0.

## Workload

`aws:redshiftserverless:Namespace` → `aws:redshiftserverless:Workgroup` (dependsOn the
namespace). Applied with the executor's live gate open, all tagged `iap:managed=true` /
`iap:planId` / `iap:resourceId`. The executor orders topologically (namespace first on create)
and reverses on destroy (workgroup first — a namespace cannot be deleted while a workgroup still
references it).

## The 9 lifecycle tests

| #   | Test                   | Result | Evidence |
| --- | ---------------------- | ------ | -------- |
| 1   | **Create**             | PASS   | Namespace then workgroup created in topological order. ARNs: `arn:aws:redshift-serverless:eu-central-1:REDACTED:namespace/e1896e97-946f-47d3-a22e-0f25d213b93f`, `…:workgroup/ded55a2f-3db3-4ad9-afe7-15bec0b8666f`. Endpoint `infraasprompt-1784345754-wg.REDACTED.eu-central-1.redshift-serverless.amazonaws.com:5439`. Namespace reached AVAILABLE, workgroup CREATING→AVAILABLE (~few min). Live verified: dbName `iapdb`, adminUsername `iapadmin`, baseCapacity 8 RPU, publiclyAccessible false. **Managed admin secret** `arn:aws:secretsmanager:…:secret:redshift!infraasprompt-1784345754-ns-iapadmin-ufX8br` exists with `OwningService=redshift` — no plaintext password anywhere (no `adminUserPassword` in any call, projection, or report). All three `iap:*` tags present on both resources. |
| 2   | **No-op**              | PASS   | Immediate re-plan + re-apply (gate open) → both `no-op` "present and converged", `errors: []`. Unpinned kmsKeyId (live `AWS_OWNED_KMS_KEY`) read as no drift (desired-gated). |
| 3   | **Safe update**        | PASS   | workgroup `publiclyAccessible` false→true→false, each an in-place `UpdateWorkgroup` (namespace `no-op`). Live-verified True after the first flip, False after the second; MODIFYING→AVAILABLE each cycle; re-plan `no-op`. baseCapacity kept at 8 RPU deliberately (16 RPU would cost more). |
| 4   | **Replacement update** | PASS (execution JUSTIFIED-DEFERRED) | namespace dbName `iapdb`→`analytics` (create-only identity) planned `replace` ("present but immutable attribute drifted — will replace (delete+create)"). Replace gate CLOSED → REFUSED verbatim, exit 3, zero mutation (live dbName still `iapdb`): `refusing to replace infraasprompt-1784345754-ns.aws:redshiftserverless:Namespace: immutable attribute drift requires delete+create (destructive); re-run with the replacement gate open`. Execution deferred — a namespace delete+create (and the dependent workgroup recreate) is many minutes at the priciest tier; the roadmap-mandated real replacement was already discharged at M22.2 (DynamoDB keySchema). |
| 5   | **Drift**              | PASS   | Out-of-band `aws redshift-serverless update-workgroup --publicly-accessible` → live true; read-only plan classified `update` on the workgroup (namespace `no-op`); live value still `true` after the plan (read-only proven); apply reconciled → live `false`; re-plan `no-op`. |
| 6   | **Controlled failure** | PASS   | `iap:managed` stripped from both (untag-resource) → destroy REFUSED both, exit 3, zero deletion, reversed topology (workgroup → namespace): `refusing to delete <logicalId>: not tagged iap:managed=true (managed-only destroy)`. |
| 7   | **Recovery**           | PASS   | `iap:managed=true` restored on both (tag-resource) → gate-closed dry-run `no-op`, `errors: []`, exit 0; destroy plan classifies both "managed — will delete" (workgroup first). |
| 8   | **Destroy**            | PASS   | Reversed topology (workgroup → namespace). Pass 1: DeleteWorkgroup accepted (→ deleting); namespace delete rejected while the workgroup still existed — live AWS ordering constraint: "Delete the associated workgroup infraasprompt-1784345754-wg before you delete the namespace." Polled workgroup to ResourceNotFoundException (~1 min), re-ran destroy → workgroup `no-op` (already absent), namespace deleted (DELETING→gone ~1.5 min). Final destroy `errors: []`, driver exit 0. |
| 9   | **Cleanup verify**     | PASS   | get-namespace / get-workgroup → ResourceNotFoundException. Managed admin secret gone (deleted with the namespace). Run-scoped tag sweep (`iap:managed=true` filtered to `1784345754`) → `[]`. **Zero run-scoped orphans.** |

## Real bugs caught by live execution (mock tests could not)

None — the handlers behaved to spec throughout: the Secrets Manager-managed admin password
(`manageAdminPassword=true`, `OwningService=redshift`, zero plaintext), the desired-gated kmsKeyId
(AWS-owned default never read as drift), the 8-RPU serverless floor, publiclyAccessible in-place
updates, the immutable-identity replacement gate (namespace dbName), the managed-only destroy
gate, and the reversed-topology teardown that respects the workgroup-outlives-namespace-deletion
ordering — which real AWS enforced explicitly ("Delete the associated workgroup … before you
delete the namespace"), confirming the dependsOn reversal is correct and load-bearing.

## Honest scope notes

- **Zero queries.** Redshift Serverless bills RPU on query-seconds; no query was run, so compute
  ≈ $0. baseCapacity was held at the 8-RPU floor (never raised to 16) for the same reason.
- **Replacement execution deferred** (test 4) — classification, gate-closed refusal, and
  zero-mutation proven live; the destructive delete+create itself was justified-deferred on
  wall-clock/cost grounds (M22.2 discharged the mandated execution).
- **No mapping emission** for the warehouse Database branch yet — per-branch outputs grammar gap
  (deferred like M22.1/M22.2/M22.3); this run drove the executor directly with a hand-built
  `ProviderPlan`. The managed admin secret ARN is the branch's intended `connectionSecret` output.
- **AWS-owned KMS** (StorageEncrypted uses the default key, no customer CMK); single workgroup;
  default network posture; enhancedVpcRouting off.
- **Parallel-wave sweep:** 5 sibling M23.2 per-service runs were live in the same account/region,
  so the region-global `live:sweep` was NOT run here — cleanup verification is run-scoped by run
  id (`1784345754`); the coordinator runs the global sweep after the wave.

## Cost

Compute is RPU billed on query-seconds; **zero queries were run**, so compute ≈ $0 despite the
8-RPU floor (the RPU-hr price applies only WHILE querying — the idle warehouse over its
~30 min create/exercise/destroy lifetime accrued no query-seconds). Storage on an empty encrypted namespace
+ the Redshift-managed admin secret, prorated over the run, are sub-cent. **Estimated: ~$0.00 for
this service run.** Wave running total finalized at closeout (M24.4); ceiling remains **<$25**.
