# M21.2 — ALB TargetGroup: LIVE RUN EVIDENCE (service 4 of 4)

**Date:** 2026-07-16 · **Status:** COMPLETE — executed against **real AWS** and fully torn down.
**Account:** REDACTED · **Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess`
(SSO AdministratorAccess) · **Run id:** `jarvis-1784206236`.

This is genuine live evidence, not a mock. The real `AwsExecutor` drove the new
`aws:elasticloadbalancing:TargetGroup` handler (M21.2) through all nine lifecycle tests — the
only M21.2 service with **no N/A rows**: safe update AND gated replacement both exercised live,
plus live default-VPC resolution (ADR-0005).

## Workload

Synthetic single-resource provider plan: `aws:elasticloadbalancing:TargetGroup`
(`jarvis-1784206236-tg`, targetType `ip`, HTTP, health check `/healthz` @ `30s`), tagged
`iap:managed=true` / `iap:planId` / `iap:resourceId`. No `vpcId` attribute — the handler
resolved the account's **default VPC live via EC2 DescribeVpcs** (ADR-0005). Executor-driven
(see honest scope).

## The 9 lifecycle tests

| #   | Test                   | Result | Evidence                                                                                                                                       |
| --- | ---------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅     | Default VPC resolved live; TargetGroup ARN `…:targetgroup/jarvis-1784206236-tg/…`; duration `30s` coerced to 30; exit 0                          |
| 2   | **No-op**              | ✅     | Immediate re-apply → `no-op`, `applied: false` (default-VPC comparison rule holds: unpinned vpcId does not read as drift)                       |
| 3   | **Safe update**        | ✅     | `healthCheckPath` `/healthz`→`/live` → `update` (ModifyTargetGroup); live `HealthCheckPath` verified `/live` by direct describe                  |
| 4   | **Replacement update** | ✅     | Port 80→8080 (ELBv2 create-only) → plan `replace`; gate CLOSED → refusal exit 3, nothing mutated; gate OPEN → delete+create, new group live on port 8080, `errors: []` |
| 5   | **Drift**              | ✅     | Out-of-band `modify-target-group` interval 30→60 → read-only plan classifies `update`                                                            |
| 6   | **Controlled failure** | ✅     | Out-of-band strip of `iap:managed` → destroy REFUSED ("managed-only destroy"), exit 3, **no silent success**                                    |
| 7   | **Recovery**           | ✅     | Managed tag restored → re-apply converged (`update` healed the interval drift, `errors: []`, exit 0)                                             |
| 8   | **Destroy**            | ✅     | `delete` applied, exit 0                                                                                                                        |
| 9   | **Cleanup verify**     | ✅     | `describe-target-groups --names` → TargetGroupNotFound; harness sweep → PASS. **Zero orphans.**                                                 |

## Real bugs caught by live execution (mock tests could not)

None — the handler passed all nine stages on first live contact, including the two behaviors
most likely to bite live: default-VPC resolution (returned the account's real
`vpc-…` and created successfully) and same-name recreate during replacement (works because
delete completes synchronously before CreateTargetGroup).

## Honest scope notes

- **Executor-driven synthetic plan**, not the CLI mapping path: the mapping realizes
  `TargetGroup` only alongside `aws:ecs:Service` (handler lands M21.3); the full CLI path is
  exercised at M21.3's three-tier run.
- The target group carried no registered targets (no instances/IPs) — attachment semantics
  arrive with ECS/ALB in M21.3.
- Default VPC usage is the recorded ADR-0005 pragmatism; the M23.4 retrofit moves this onto
  IaP-managed `Network`.

## Cost

Target groups are free (billing attaches to load balancers, none created); resource live ~6
minutes. **This run: $0.00.** **Roadmap running total: $0.00 of the $25.00 ceiling.**
