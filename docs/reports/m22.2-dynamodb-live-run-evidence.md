# M22.2 — AWS DynamoDB Table (key-value Database): LIVE RUN EVIDENCE (service 1 of 4)

**Date:** 2026-07-17 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `infraasprompt-1784279604`.

One on-demand table `infraasprompt-1784279604-tbl` (keySchema `pk:S`, PAY_PER_REQUEST, deletion
protection off, SSE unpinned — the AWS-owned default key never reads as drift). This run
executed the **roadmap-mandated first data-engine replacement** (immutable keySchema →
gated delete+create).

| #   | Test                   | Result | Evidence                                                                                   |
| --- | ---------------------- | ------ | -------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅     | CreateTable → ACTIVE on first poll (<5s); ARN recorded; `iap:managed`/`iap:planId`/`iap:resourceId` tags verified via list-tags-of-resource |
| 2   | **No-op**              | ✅     | Immediate re-plan + re-apply → `no-op`, "present and converged"                            |
| 3   | **Safe update**        | ✅     | deletionProtection `false`→`true` planned `update`, applied, live-verified `true`; reverted `true`→`false` the same way (both directions recorded) |
| 4   | **Replacement update** | ✅     | keySchema `pk:S`→`pk:S,sk:S` planned `replace`. Gate CLOSED → REFUSED, exit 3, zero mutation: "refusing to replace infraasprompt-1784279604-tbl.aws:dynamodb:Table: immutable attribute drift requires delete+create (destructive); re-run with the replacement gate open". Gate OPEN → DeleteTable issued, then CreateTable hit the documented race ("Table already exists: infraasprompt-1784279604-tbl", exit 3) while the old name was DELETING; name released in <10s (describe → ResourceNotFoundException), re-apply classified `create` (DELETING reads as absent), new table ACTIVE ~10s later with live KeySchema pk HASH + sk RANGE (both S); post-replace re-plan `no-op`; replacement table re-tagged correctly |
| 5   | **Drift**              | ✅     | Out-of-band `update-table --deletion-protection-enabled` via CLI → read-only plan `update`; live value still drifted after plan (read-only proven); apply reconciled → live `false` |
| 6   | **Controlled failure** | ✅     | `iap:managed` tag stripped via untag-resource → destroy REFUSED, exit 3, table untouched: "refusing to delete infraasprompt-1784279604-tbl.aws:dynamodb:Table: not tagged iap:managed=true (managed-only destroy)" |
| 7   | **Recovery**           | ✅     | Tag restored → destroy plan classifies "managed — will delete"; non-destroy dry-run `no-op`, `errors: []`, exit 0 |
| 8   | **Destroy**            | ✅     | DeleteTable via destroy apply with confirm gate, `errors: []`, exit 0                       |
| 9   | **Cleanup verify**     | ✅     | describe-table → ResourceNotFoundException; run-scoped tag sweep (`iap:managed=true` filtered to `1784279604`) → 0 matches; list-tables name check → none. **Zero run-scoped orphans.** |

**Replacement race (recorded honestly):** the executor's replace path is delete-then-create in
one pass; DynamoDB holds the table name while status is DELETING, so the create raced it and
failed with ResourceInUseException — exactly the behaviour the handler documents as "the honest
signal to retry after the deletion completes". The name released in under 10 seconds and the
re-apply converged cleanly. Not a bug; a waiter between delete and create is a possible future
ergonomics improvement.

**Bugs:** none — the handler behaved to spec throughout, including the desired-gated SSE
posture (unpinned plan never saw the AWS-owned default key as drift) and deletion-protection
handling.

**Honest scope notes:** no mapping emission for the key-value Database kind yet — per-branch
outputs grammar gap, deferred like M22.1 apigatewayv2; this run drove the executor directly
with a hand-built ProviderPlan. Single table only (no GSI/LSI, no provisioned billing, no
customer-KMS SSE). Sibling M22.2 runs (Timestream, KMS, Backup) were live in the same
account/region, so the region-global `live:sweep` was NOT run here — cleanup verification was
run-scoped by run id; the coordinator runs the global sweep after the wave.

**Cost: ~$0.00** (on-demand table, zero items, zero storage, ~30 min lifetime). Wave running
total finalized at closeout; M22.1 baseline ~$0.08/$25.
