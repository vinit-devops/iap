# M19.3 — Real AWS Golden-Path Runbook (BUILD DONE — awaiting approval to go live)

**Status:** execution layer BUILT and locally verified against a mocked SDK. **No live AWS
call has been made.** This runbook is the exact sequence I would run **only after your explicit
approval**. Every mutating step is gated behind `--confirm`.

## What is built (all mock-tested, zero live calls)

- `@iap/deploy-aws` — real `AwsExecutor` over `@aws-sdk` for **S3 / SQS / IAM** (IaP kinds
  ObjectStore / Queue / Identity). Dry-run by default; mutates only with `apply: true`;
  mandatory `iap:managed=true` / `iap:planId` / `iap:resourceId` tags; **managed-only destroy**
  (refuses any resource not tagged `iap:managed`). 23 mock tests.
- `@iap/state` `FileStateBackend` — durable, sha256 integrity (fail-closed), cross-process
  locking, optional AES-256-GCM at-rest encryption. 19 tests.
- `iap deploy` / `destroy` / `drift` / `state` CLI commands wired to the above, `--confirm`-gated.

## Preconditions I require before going live (safety)

1. **Profile:** `REDACTED_AdministratorAccess` ONLY (reachable; verified via STS — acct
   REDACTED, SSO). ⚠️ This is broad admin. I strongly recommend a **dedicated sandbox +
   restricted IAM role** scoped to just S3/SQS/IAM in one region, not open AdministratorAccess.
2. **Region:** one region, explicitly set (e.g. `--region us-east-1`).
3. **Cost:** these resources are ~free (empty S3 bucket, idle SQS queue, IAM role) — negligible,
   but a budget alarm is still advisable.
4. **Tags:** the executor stamps mandatory tags on every create; destroy touches only tagged
   resources. No pre-existing resource is ever modified.
5. **Blast radius:** unique, run-scoped resource names; no production data; teardown at the end.

## The narrow golden path (one workload)

An IaP doc with an `ObjectStore` + `Queue` + `Identity` → mapped to `aws:s3:Bucket`,
`aws:sqs:Queue`, `aws:iam:Role`.

## The 9 required lifecycle tests → exact commands

> All commands use `--profile REDACTED_AdministratorAccess --region <region>
--mapping <aws provider> --state ./.iap-state`. Mutations require `--confirm`.

| #   | Test                   | Command                                                                           | Expected                                                 |
| --- | ---------------------- | --------------------------------------------------------------------------------- | -------------------------------------------------------- |
| 1   | **Create**             | `iap deploy -f infra.iap.yaml … --confirm`                                        | 3 resources created, tagged `iap:managed`                |
| 2   | **No-op**              | `iap deploy … --confirm` (re-run)                                                 | all `no-op` (idempotent), state unchanged                |
| 3   | **Safe update**        | edit a mutable attr (e.g. SQS `messageRetentionPeriod`), `iap deploy … --confirm` | `update-in-place`, no replacement                        |
| 4   | **Replacement update** | change an immutable attr (e.g. bucket name / FIFO), `iap deploy … --confirm`      | `replace` (delete+create), state reflects new identity   |
| 5   | **Drift**              | mutate a resource out-of-band (console/CLI), `iap drift …`                        | drift detected + reported, read-only                     |
| 6   | **Controlled failure** | force a failure (e.g. name collision / denied action), `iap deploy … --confirm`   | fail-closed, partial outcome recorded, no silent success |
| 7   | **Recovery**           | fix the cause, `iap deploy … --confirm`                                           | converges to desired, clean outcome                      |
| 8   | **Destroy**            | `iap destroy … --confirm`                                                         | only `iap:managed` resources deleted                     |
| 9   | **Cleanup verify**     | `iap drift …` / AWS describe                                                      | zero managed resources remain, no orphans                |

## Live sign-off checklist (I will not proceed until all checked)

- [ ] Approver explicitly authorizes a LIVE run.
- [ ] Region confirmed.
- [ ] (Recommended) restricted sandbox IAM role in place, or explicit acceptance of using
      AdministratorAccess for this narrow S3/SQS/IAM run.
- [ ] Teardown (test 8–9) will run at the end of the session.

**Until then: no live API call. The build is complete and locally verified; the switch is yours.**
