# M23.5 — Amazon Data Firehose (DeliveryStream + sibling S3 bucket + IAM role): LIVE RUN EVIDENCE (service 2 of 3-live)

**Date:** 2026-07-18 · **Status:** COMPLETE — executed against **real AWS** and fully torn down.
**Account:** REDACTED · **Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `infraasprompt-1784351930`.

Genuine live evidence, not a mock. The CLI executor drove a DirectPut→S3 Firehose delivery stream end to end — created live on its IaP-wired S3 destination bucket and firehose-trust IAM role, put through all nine lifecycle tests, then destroyed in reversed topology with zero run-scoped orphans.

## Headline

**Firehose delivery stream live with its IaP-wired S3 destination + firehose-trust IAM role.** Three resources, sequential id/ARN threading (output-binding grammar deferred): the bucket ARN is deterministic, the role ARN was captured from the role apply report identifier and cross-checked byte-equal to the `arn:aws:iam::<account>:role/<resourceId>` iam.ts convention, then fed into the delivery stream's `S3DestinationConfiguration`.

## Workload (created live, sequential)

| Order | Resource                           | Type                                                                                                                                                     | Live identifier                                                                     | iap tags (verified via live describe) |
| ----- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------- |
| 1     | `infraasprompt-1784351930-dest`    | `aws:s3:Bucket` — private (AES256, versioning Enabled, public-access blocked; **empty, no objects**)                                                     | `arn:aws:s3:::infraasprompt-1784351930-dest`                                        | ✅ managed/planId/resourceId          |
| 2     | `infraasprompt-1784351930-fh-role` | `aws:iam:Role` — trust `firehose.amazonaws.com`, inline s3:PutObject/AbortMultipartUpload/GetBucketLocation/ListBucket/PutObjectAcl on the bucket + `/*` | `arn:aws:iam::REDACTED:role/infraasprompt-1784351930-fh-role`                       | ✅ managed/planId/resourceId          |
| 3     | `infraasprompt-1784351930-fh`      | `aws:firehose:DeliveryStream` — DirectPut → `S3DestinationConfiguration` (bucket + role wired)                                                           | `arn:aws:firehose:eu-central-1:REDACTED:deliverystream/infraasprompt-1784351930-fh` | ✅ managed/planId/resourceId          |

The delivery stream reached **ACTIVE** and its live `S3DestinationDescription` reported the wired bucket ARN + role ARN back. It created on the **first** attempt — **no IAM-propagation retry was needed** this run (the bucket+role applies preceded it, giving the role time to propagate; the `apply-fh` mode carries a bounded retry on the "cannot assume role" case, which was never triggered). `requireDestination` fail-closed (bucket ARN + role ARN both REQUIRED) is covered by the mock suite; not re-exercised destructively live.

## The 9 lifecycle tests

| #   | Test                   | Result                                     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ---------------------- | ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅                                         | Read-only full plan → 3× `create`. Sequential applies: bucket `applied:true`, role `applied:true` (captured ARN == deterministic ARN, verified equal), delivery stream `applied:true` (`create`, ARN returned). Stream polled to **ACTIVE**, DirectPut, live destination = wired bucket+role. `iap:managed`/`iap:planId`/`iap:resourceId` verified on all three via `list-tags-for-delivery-stream` / `list-role-tags` / `get-bucket-tagging`.                                                                                                                                                                                      |
| 2   | **No-op**              | ✅                                         | Immediate re-plan of the firehose → `no-op`; gate-closed apply (dry-run) → `no-op`, `applied:false`, zero mutating calls. Full-plan re-plan: bucket `no-op`, stream `no-op`; the **role reads `update`** — a designed-in, benign quirk: iam.ts does not enumerate inline policies on read, so a non-empty desired `inlinePolicy` always classifies `update` and reconciles idempotently (PutRolePolicy). Not drift in the stream (the focus).                                                                                                                                                                                       |
| 3   | **Safe update**        | ✅ JUSTIFIED N/A                           | The Firehose handler projects only `deliveryStreamType` (immutable), `destinationBucketArn`, `roleArn` — **no free-form mutable scalar** (no prefix/buffering in the projection). A safe in-place change would mean repointing a sibling ARN, which needs a _second_ sibling — out of the approved single-stream/bucket/role scope. The `UpdateDestination` in-place path is covered by the mock suite. The update-in-place code path was instead exercised live against the sibling bucket in test 5.                                                                                                                              |
| 4   | **Replacement update** | ✅ (gate) / JUSTIFIED-DEFERRED (execution) | Desired `deliveryStreamType=KinesisStreamAsSource` vs live `DirectPut` → immutable drift planned `replace`. Replacement gate CLOSED → **REFUSED verbatim**, `applied:false`, errors present (exit 3): `refusing to replace infraasprompt-1784351930-fh.aws:firehose:DeliveryStream: immutable attribute drift requires delete+create (destructive); re-run with the replacement gate open`. Stream verified untouched (DirectPut, ACTIVE). Execution deferred: a KinesisStreamAsSource stream needs a Kinesis stream source (different resource + cost), and the replacement mandate already executed for real at M22.2 (DynamoDB). |
| 5   | **Drift**              | ✅                                         | Induced on the required sibling bucket (the stream has no in-scope mutable projected attribute): out-of-band `aws s3api put-bucket-versioning --status Suspended` → read-only plan classified `update`; **live still Suspended after the read-only plan** (read-only proven); apply reconciled via PutBucketVersioning → Enabled; re-plan `no-op`.                                                                                                                                                                                                                                                                                  |
| 6   | **Controlled failure** | ✅                                         | `iap:managed` stripped from the stream via `untag-delivery-stream` → fh-scoped destroy **REFUSED verbatim**, `applied:false`, exit 3: `refusing to delete infraasprompt-1784351930-fh.aws:firehose:DeliveryStream: not tagged iap:managed=true (managed-only destroy)`. Stream untouched (ACTIVE). Destroy deliberately scoped to an fh-only plan (apply collects per-object errors and continues, so a full-plan destroy would have torn down the managed siblings past the refusal).                                                                                                                                              |
| 7   | **Recovery**           | ✅                                         | Tag restored via `tag-delivery-stream` → gate-closed dry-run (full) converged: bucket `no-op`, stream `no-op`, role benign `update`, `errors: []`, exit 0. Destroy-plan classified all three deletable **in reversed topology (fh → role → s3)**.                                                                                                                                                                                                                                                                                                                                                                                   |
| 8   | **Destroy**            | ✅                                         | Full destroy, reversed topology: delivery stream → IAM role → S3 bucket, all `applied:true`, `errors: []`, exit 0. Clean single pass — the role and (empty) bucket deleted successfully even while the stream was still async-DELETING; no disable-settle race (Firehose delete is one call, unlike M22.5 Batch JQ/CE). Bucket empties trivially (no objects were ever put).                                                                                                                                                                                                                                                        |
| 9   | **Cleanup verify**     | ✅                                         | `describe-delivery-stream` → `ResourceNotFoundException`; `get-role` → `NoSuchEntity`; `head-bucket` → `404`; run-scoped tagging-API sweep (`iap:managed=true` filtered to `1784351930`) → `[]`. **Zero run-scoped live orphans.**                                                                                                                                                                                                                                                                                                                                                                                                  |

## Real bugs caught by live execution (mock tests could not)

**None.** No product-code bug surfaced. `packages/deploy-aws/src/firehose.ts` and `packages/deploy-aws/test/m23.5-firehose.test.ts` were **not** modified — the create/no-op/update/replace/destroy paths behaved exactly as the mock suite predicts.

## Honest scope notes

- **Mid-run token expiry + recovery (operational, not a product fault).** After the three creates, the profile's temporary STS session token expired (`ExpiredTokenException`) during the ACTIVE poll; per the run's robustness protocol the run STOPPED immediately and attempted teardown (which also failed on the dead token — recorded honestly). The SSO session then refreshed; the run resumed with valid credentials, completed test 1's ACTIVE/tag verification, and ran tests 2–9 to a clean teardown. No resource was left orphaned.
- No records were ever `PutRecord`'d and **no objects were put in the bucket** — $0 by construction (Firehose is pay-per-data-ingested; none flowed).
- Test 3 is a justified N/A and test 4's replacement execution is deferred (see the table) — the only immutable projection key is `deliveryStreamType`, and converting DirectPut↔KinesisStreamAsSource requires a Kinesis source.
- No mapping emission for Firehose kinds yet — the executor was driven directly with a hand-built ProviderPlan (output-binding grammar gap, deferred like M22.1 apigatewayv2 / M22.5 Batch).
- 2 sibling M23.5 runs (Kinesis, OpenSearch) were live in parallel in the same account/region, so the region-global `live:sweep` was **not** run here; cleanup verification was run-scoped by run id. The coordinator sweeps after the wave.

## Cost

**$0.00** — zero records ingested, zero objects stored; the delivery stream, empty bucket, and idle role carry no idle/hourly charge (~short lifetime, interrupted by the token refresh). Running total effectively unchanged against the **<$25** roadmap ceiling; reconciled at closeout (M24.4). Cost Explorer actuals lag ~24 h.
