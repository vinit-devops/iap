# M22.1 — AWS SSM Parameter (standalone Secret): LIVE RUN EVIDENCE (service 2 of 8)

**Date:** 2026-07-16 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `infraasprompt-1784216089`.

SecureString parameter with a locally generated value (never logged, never read back —
`WithDecryption` is never set).

| #   | Test                   | Result | Evidence                                                                                   |
| --- | ---------------------- | ------ | -------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅     | PutParameter SecureString, generated value                                                  |
| 2   | **No-op**              | ✅     | Re-apply → `no-op`                                                                         |
| 3   | **Safe update**        | ⚠️ N/A | Justified: the value is write-only and type is the only projection key (immutable) — tags are the only in-place surface |
| 4   | **Replacement update** | ✅     | Type SecureString→String → plan `replace`; gate CLOSED → exit 3; gate OPEN → delete+create executed; live `Type=String` verified |
| 5   | **Drift**              | ✅     | Out-of-band `delete-parameter` → read-only plan `create` (absence drift)                    |
| 6   | **Controlled failure** | ✅     | Tag stripped → destroy REFUSED, exit 3                                                     |
| 7   | **Recovery**           | ✅     | Recreate + tag restore → converged                                                         |
| 8   | **Destroy**            | ✅     | DeleteParameter, exit 0                                                                    |
| 9   | **Cleanup verify**     | ✅     | ParameterNotFound; sweep PASS. **Zero orphans.**                                           |

**Bugs:** none. **Cost: $0.00** (standard-tier parameter). Running total ~$0.08/$25.
