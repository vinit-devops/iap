# M22.1 — AWS Lambda (Function): LIVE RUN EVIDENCE (service 6 of 8)

**Date:** 2026-07-16 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `infraasprompt-1784216685`
(shared with the Scheduler and API Gateway runs — the serverless trio).

Zip-package function (code staged in a run-scoped bucket) with its sibling execution role
created through the upgraded IAM handler (multi-service trust: lambda + scheduler on one role).

| #   | Test                   | Result                 | Evidence                                                                                                                                                                            |
| --- | ---------------------- | ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅                     | Role (array-Principal trust) then function created; `lambda invoke` returned **`{"ok":true,"from":"infraasprompt-live"}`** — real execution                                         |
| 2   | **No-op**              | ✅                     | Re-apply → `no-op`                                                                                                                                                                  |
| 3   | **Safe update**        | ✅                     | memorySize 128→256 → `update` (UpdateFunctionConfiguration)                                                                                                                         |
| 4   | **Replacement update** | ⚠️ partial (justified) | packageType Zip→Image → plan `replace` + gate-closed refusal exit 3 LIVE; gated execution justified-deferred (an Image function needs a same-account ECR image — ECR arrives M23.2) |
| 5   | **Drift**              | ✅                     | (via the trio's shared config drift checks; timeout/memory reads round-trip)                                                                                                        |
| 6   | **Controlled failure** | ✅                     | Tag stripped → destroy REFUSED, exit 3                                                                                                                                              |
| 7   | **Recovery**           | ✅                     | Tag restored → destroy converged                                                                                                                                                    |
| 8   | **Destroy**            | ✅                     | Function deleted, then the role (inline policy removed first — upgraded delete path proven live)                                                                                    |
| 9   | **Cleanup verify**     | ✅                     | ResourceNotFoundException + NoSuchEntity; staging bucket removed; sweep PASS. **Zero orphans.**                                                                                     |

**Bugs:** none in the Lambda handler. The IAM upgrades (array-Principal trust; DeleteRolePolicy
before DeleteRole) both held on first live contact. **Cost: ~$0.00** (two invocations,
sub-second). Running total ~$0.08/$25.
