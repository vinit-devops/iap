# M22.5 — AWS Auto Scaling Group (elastic fleet): LIVE RUN EVIDENCE (service 2 of 6)

**Date:** 2026-07-17 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `infraasprompt-1784289037`.

Two-resource dependency chain: `infraasprompt-1784289037-lt` (aws:ec2:LaunchTemplate, AL2023 arm64 AMI
via SSM, t4g.nano) → `infraasprompt-1784289037-asg` (aws:autoscaling:AutoScalingGroup, min 0 / max 1 /
**desired 0** — the zero-capacity, zero-cost default posture), the ASG carrying `dependsOn` on the
LT's logicalId.

**HEADLINE — dependsOn topological ordering proven live, both directions.** This is the first
live proof of the fix for the M22.2 ordering finding (dependents must be created after and
destroyed before their dependencies). The proof is airtight because alphabetical order argues the
OTHER way: `…-asg` sorts before `…-lt`, and the driver even listed the ASG first in
`plan.resources` — yet the apply report created **LT → ASG** and the destroy report deleted
**ASG → LT** (reversed topology), both orders verified in the report `items[]`. Only the
`dependsOn` topology can produce that ordering.

| #   | Test                   | Result             | Evidence                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | ---------------------- | ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅                 | Apply report ordered LT create **before** ASG create (against alphabetical); both applied in ~4s, `errors: []`. ASG ARN `arn:aws:autoscaling:…:autoScalingGroup:…:autoScalingGroupName/infraasprompt-1784289037-asg`; LT `lt-03f4eec1ec4e1d92d`. Live describe: min 0 / max 1 / desired 0, **Instances: 0** ($0 posture), LT wired at `$Default`, all three `iap:*` tags present with **PropagateAtLaunch=true** |
| 2   | **No-op**              | ✅                 | Immediate re-plan → both `no-op` "present and converged"; re-apply `no-op`/applied=false, `errors: []`                                                                                                                                                                                                                                                                                                           |
| 3   | **Safe update**        | ✅                 | max `1`→`2` planned `update` (LT `no-op`), applied via UpdateAutoScalingGroup, live-verified MaxSize=2. Then the real-instance exercise: desired `0`→`1` → one **t4g.nano** `i-0f8a85700bcd5cec2` Pending at +10s, **InService at +17s**; desired `1`→`0` → instance drained out of the group in ~70s. Total instance lifetime ≈90s (cost: fractions of a cent)                                                  |
| 4   | **Replacement update** | ⚠️ N/A (justified) | The handler declares **no immutableProjectionKeys**: every projected attribute — min, max, desired, and **launchTemplateName itself** — mutates in place via UpdateAutoScalingGroup, so no drift can ever classify as the gated delete+create replace (ADR-0006 has nothing to gate). Same posture as the sibling LaunchTemplate handler (versioned convergence)                                                 |
| 5   | **Drift**              | ✅                 | Out-of-band `aws autoscaling update-auto-scaling-group --max-size 3` → read-only plan classified `update` ("present but drifted — will reconcile"); live MaxSize still 3 after the plan (read-only proven); apply reconciled to 2; re-plan fully `no-op`                                                                                                                                                         |
| 6   | **Controlled failure** | ✅                 | `iap:managed` stripped (autoscaling delete-tags on the ASG; also from the LT — see note below) → destroy REFUSED both, exit 3, zero mutation: "refusing to delete infraasprompt-1784289037-asg.aws:autoscaling:AutoScalingGroup: not tagged iap:managed=true (managed-only destroy)" (and the LT twin); both resources verified still present                                                                    |
| 7   | **Recovery**           | ✅                 | Tags restored → destroy plan classifies both "managed — will delete"; non-destroy dry-run all `no-op`, `errors: []`, exit 0                                                                                                                                                                                                                                                                                      |
| 8   | **Destroy**            | ✅                 | Report order **ASG first, LT second** — reversed topology, the other half of the ordering proof. ASG deleted with **ForceDelete=true**, then DeleteLaunchTemplate; `errors: []`, exit 0, ~2s                                                                                                                                                                                                                     |
| 9   | **Cleanup verify**     | ✅                 | describe-auto-scaling-groups → 0 groups (drained ~90s after ForceDelete); describe-launch-templates → InvalidLaunchTemplateName.NotFoundException; run-scoped tag sweep (`iap:managed=true` filtered to `1784289037`) → 0; describe-instances by `iap:planId=infraasprompt-1784289037` → 0. **Zero run-scoped orphans.**                                                                                         |

**Test-6 note (recorded honestly):** the destroy pass covers the whole plan and the executor
continues past a per-resource refusal, so stripping only the ASG's tag would have let the
still-managed LT be deleted mid-refusal-test (a half-teardown). The tag was therefore stripped
from **both** resources so the controlled failure refused the entire plan without mutation; both
tags were restored for test 7.

**Bugs:** none — the handler pair behaved to spec throughout, including the fail-closed
`launchTemplateName`/`imageId` requirements and the delete-in-progress-reads-as-absent posture
(never exercised: teardown was clean on the first pass).

**Honest scope notes:** zero-capacity default posture means create alone proves no instance can
actually boot — test 3's desired 0→1→0 round-trip is what proved the LT data launches a real
instance. ForceDelete semantics (desired>0 teardown terminates in-flight instances with the
group) were not separately exercised — desired was already 0 at destroy. Default-VPC subnet
placement (ADR-0005). No mapping emission for this kind yet — this run drove the executor
directly with a hand-built ProviderPlan (same gap as M22.2 DynamoDB). 14 sibling M22.5/parallel
runs were live in the same account/region (an EC2 sibling also creates launch templates), so the
region-global `live:sweep` was NOT run here — cleanup verification was strictly run-scoped by run
id; the coordinator runs the global sweep after the wave.

**Cost: ≈$0.00–0.01** (ASG and LT are free; one t4g.nano for ~90 seconds ≈ $0.0001; run lifetime
~5 min). Wave running total finalized at closeout; baseline ≈$0.09/$25.
