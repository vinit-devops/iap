# M22.1 — Amazon SNS (Topic): LIVE RUN EVIDENCE (service 1 of 8)

**Date:** 2026-07-16 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `jarvis-1784215942`.

Standard topic with AWS-managed SSE, driven by the real executor through the new
`aws:sns:Topic` handler.

| #   | Test                   | Result | Evidence                                                                                       |
| --- | ---------------------- | ------ | ---------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅     | Topic ARN live; `KmsMasterKeyId=alias/aws/sns` applied                                          |
| 2   | **No-op**              | ✅     | Re-apply → `no-op`                                                                             |
| 3   | **Safe update**        | ✅     | sseEnabled true→false → `update`; live `KmsMasterKeyId` verified absent by direct read         |
| 4   | **Replacement update** | ⚠️ N/A | Justified: `fifoTopic` is declared immutable, but the `.fifo` suffix is identity-carrying — a FIFO drift changes the lookup name and reads absent→create, never replace (same finding class as ACM domainName, M21.2; carried to the identity-pinning fix planned for M23.2) |
| 5   | **Drift**              | ✅     | Out-of-band `set-topic-attributes` (SSE on) → read-only plan `update`                          |
| 6   | **Controlled failure** | ✅     | `iap:managed` stripped → destroy REFUSED, exit 3                                               |
| 7   | **Recovery**           | ✅     | Tag restored → converged, exit 0                                                               |
| 8   | **Destroy**            | ✅     | DeleteTopic, exit 0                                                                            |
| 9   | **Cleanup verify**     | ✅     | `get-topic-attributes` → NotFound; sweep PASS. **Zero orphans.**                               |

**Bugs:** none. **Scope:** executor-driven synthetic plan; SSE uses `alias/aws/sns` (CMKs at
M22.2). **Cost: $0.00.** Running total ~$0.08/$25.
