# M21.3 — Application Load Balancer: LIVE RUN EVIDENCE (service 2 of 4)

**Date:** 2026-07-16 · **Status:** COMPLETE — executed against **real AWS** and fully torn down.
**Account:** REDACTED · **Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess`
(SSO AdministratorAccess) · **Run id:** `infraasprompt-1784208415`.

The real `AwsExecutor` drove the new `aws:elasticloadbalancing:LoadBalancer` handler through
all nine lifecycle tests, including a live gated replacement (scheme change).

## Workload

Synthetic single-resource plan: `aws:elasticloadbalancing:LoadBalancer`
(`infraasprompt-1784208415-alb`, application/internal, dropInvalidHeaderFields on, access logs off)
over the default VPC's three subnets (ADR-0005).

## The 9 lifecycle tests

| #   | Test                   | Result | Evidence                                                                                                                                             |
| --- | ---------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅     | ALB ARN `…:loadbalancer/app/infraasprompt-1784208415-alb/…`; 3 default-VPC subnets; attributes applied at create                                     |
| 2   | **No-op**              | ✅     | Immediate re-apply → `no-op` (attributes round-trip through DescribeLoadBalancerAttributes)                                                          |
| 3   | **Safe update**        | ✅     | dropInvalidHeaderFields true→false → `update`; live attribute verified `false` by direct describe                                                    |
| 4   | **Replacement update** | ✅     | scheme internal→internet-facing → plan `replace`; gate CLOSED → refusal exit 3; gate OPEN → delete+create executed, same name accepted, `errors: []` |
| 5   | **Drift**              | ✅     | Out-of-band attribute modify → read-only plan classifies `update`                                                                                    |
| 6   | **Controlled failure** | ✅     | Managed tag stripped → destroy REFUSED, exit 3, no silent success                                                                                    |
| 7   | **Recovery**           | ✅     | Tag restored → re-apply converged (`errors: []`, exit 0)                                                                                             |
| 8   | **Destroy**            | ✅     | DeleteLoadBalancer, exit 0                                                                                                                           |
| 9   | **Cleanup verify**     | ✅     | `describe-load-balancers --names` → LoadBalancerNotFound; sweep PASS. **Zero orphans.**                                                              |

## Real bugs caught by live execution (mock tests could not)

None — including the riskiest live behavior: same-name recreate during replacement worked
(ELBv2 releases the name synchronously with the delete).

## Honest scope notes

- Executor-driven synthetic plan (the mapping realizes LoadBalancer alongside the ACM cert,
  whose handler exists since M21.2; full Gateway golden path with listeners + sslPolicy is
  the M23.4 retrofit).
- No listeners were created — `sslPolicy` is a listener setting, excluded from this handler's
  projection until listener wiring lands (recorded gap).
- The mock-level fail-closed guard (accessLogsEnabled without a bucket) is mock-proven; not
  exercised live.

## Cost

One idle ALB for ~25 minutes ≈ **$0.01** (hourly rate prorated, no LCUs).
**Roadmap running total: ~$0.03 of $25.00.**
