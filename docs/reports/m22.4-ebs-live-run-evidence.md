# M22.4 — AWS EBS Volume (block storage): LIVE RUN EVIDENCE (service 1 of 3)

**Date:** 2026-07-17 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `jarvis-1784289033`.

One unattached encrypted gp3 volume `jarvis-1784289033-vol` (8 GiB, AZ derived from the first
default subnet — eu-central-1a). EBS volumes have no caller-chosen names, so this run is the
first live proof of the **tag-owned identity** model: the handler resolves its volume purely by
`iap:resourceId=<logicalId>` tag filter, and every mutation uses the read-resolved volume id.

| #   | Test                   | Result | Evidence                                                                                   |
| --- | ---------------------- | ------ | -------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅     | CreateVolume → `vol-00bb4f7e5fb41e6df` available in seconds; 8 GiB gp3, Encrypted true (account default KMS), eu-central-1a; `iap:managed`/`iap:planId`/`iap:resourceId` + `Name` tags all stamped at creation via TagSpecifications, verified by describe-volumes |
| 2   | **No-op**              | ✅     | Immediate re-plan + re-apply → `no-op`, "present and converged", and the report's `identifier` was the same `vol-00bb4f7e5fb41e6df` — the DescribeVolumes `tag:iap:resourceId` filter resolving the exact volume live IS the identity-model evidence, not just the idempotence evidence |
| 3   | **Safe update**        | ✅     | sizeGiB `8`→`16` planned `update`, ModifyVolume issued; describe-volumes-modifications showed `completed 16 8` on the FIRST poll; live size 16 GiB, re-plan `no-op`. **Shrink refused honestly:** desired `16`→`8` planned `update` but apply recorded the error verbatim — "EBS volumes cannot shrink: jarvis-1784289033-vol.aws:ec2:Volume is 16 GiB live but 8 GiB desired — refusing (raise sizeGiB or replace the volume and migrate the data manually)" — exit 3, zero ModifyVolume calls, volume untouched. Size/type were then left alone for the rest of the run (ModifyVolume ~6h per-volume cooldown: grow once only) |
| 4   | **Replacement update** | ✅     | availabilityZone pinned to eu-central-1b (live 1a) planned `replace`. Gate CLOSED → REFUSED, exit 3, zero mutation: "refusing to replace jarvis-1784289033-vol.aws:ec2:Volume: immutable attribute drift requires delete+create (destructive); re-run with the replacement gate open". Gate OPEN → DeleteVolume `vol-00bb4f7e5fb41e6df`, CreateVolume from DESIRED attrs → `vol-0e7c55506dcbac8eb` available in eu-central-1b at 16 GiB, all tags re-stamped; old id NotFound; post-replace re-plan `no-op`. No name race is possible for EBS (ids are AWS-generated) — the delete+create pass converged in one shot |
| 5   | **Drift**              | ✅     | Out-of-band `aws ec2 delete-volume` on `vol-0e7c55506dcbac8eb` → read-only plan classified `create` ("absent — will create", absence drift, M22.1 SSM precedent); apply recreated → new id `vol-07c275787da313478` (16 GiB, eu-central-1b), verified via tag-filtered describe |
| 6   | **Controlled failure** | ✅     | `iap:managed` tag stripped via delete-tags → destroy REFUSED, exit 3, volume still `available`: "refusing to delete jarvis-1784289033-vol.aws:ec2:Volume: not tagged iap:managed=true (managed-only destroy)" |
| 7   | **Recovery**           | ✅     | Tag restored via create-tags → destroy plan "managed — will delete"; non-destroy dry-run `no-op`, `errors: []`, exit 0 |
| 8   | **Destroy**            | ✅     | DeleteVolume `vol-07c275787da313478` via destroy apply, `errors: []`, exit 0                |
| 9   | **Cleanup verify**     | ✅     | describe-volumes by `iap:resourceId` tag (all states) → `[]`; `tag:Name=jarvis-1784289033-*` → `[]`; run-scoped tagging-API sweep (`iap:managed=true` filtered to `1784289033`) → 0 ARNs. **Zero run-scoped orphans.** |

**Bugs:** none — the handler behaved to spec throughout: tag-owned identity resolution, grow-only
sizeGiB with the honest shrink refusal, pinned-AZ replacement gating, and managed-only destroy
all matched the mock-tested contract (`packages/deploy-aws/test/m22.4-ebs.test.ts`) live.

**Honest scope notes:** unattached volumes only — no attach/detach, no in-use delete failure, no
snapshot lineage; the ambiguous-identity (>1 tag match) fail-closed path was not induced live
(mock-covered). The ModifyVolume ~6h per-volume cooldown means the run grew exactly once and
never retyped — a second same-run modification would have been refused by AWS, so it was not
attempted. No mapping emission for the Volume kind yet (per-branch outputs grammar gap, deferred
like M22.1 apigatewayv2) — this run drove the executor directly with a hand-built ProviderPlan.
14 sibling M22.4/M22.5-era runs were live in the same account/region in parallel, so the
region-global `live:sweep` was NOT run here — cleanup verification was run-scoped by run id; the
coordinator runs the global sweep after the wave.

**Cost: ~$0.00** (gp3 8→16 GiB, unattached, total live lifetime ~4 min across all three volume
ids ≈ $0.001–0.002 at eu-central-1 gp3 rates). Wave running total finalized at closeout;
baseline entering M22.4 ~$0.09/$25.
