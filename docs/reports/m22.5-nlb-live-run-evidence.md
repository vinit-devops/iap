# M22.5 — AWS Network Load Balancer (NLB variant): LIVE RUN EVIDENCE (service 5 of 6)

**Date:** 2026-07-17 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `jarvis-1784289040`.

One internal NLB `jarvis-1784289040-nlb` on the **same target type as the live-proven M21.3
ALB** (`aws:elasticloadbalancing:LoadBalancer`; `loadBalancerType: 'network'` discriminates the
variant). Projection is `{loadBalancerType, scheme}` only — both fixed at creation. This run
live-proved the NLB read path (no HTTP-attribute describe → no phantom drift) and the
**fail-closed ALB-only-attribute guard** (§12.3 no-silent-drop).

| #   | Test                   | Result | Evidence                                                                                   |
| --- | ---------------------- | ------ | -------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅     | CreateLoadBalancer Type `network`, Scheme `internal` over the default VPC's 3 subnets (≥2-AZ rule) → `provisioning`→`active` in ~110s; ARN `…:loadbalancer/net/jarvis-1784289040-nlb/f9502a14c778e5ae`; live Type verified `network`; `iap:managed`/`iap:planId`/`iap:resourceId` all present via describe-tags |
| 2   | **No-op**              | ✅     | Immediate re-plan + gate-closed dry-run → `no-op`, "present and converged". The NLB read branch **skips DescribeLoadBalancerAttributes entirely** — the ALB-only HTTP surfaces (`routing.http.*`, access logs) can never leak into the projection, so no phantom drift is possible; the live `no-op` proves it (a leak would have classified `update`) |
| 3   | **Safe update**        | ⚠️ justified N/A + fail-closed guard live-proven | The NLB projection is `{loadBalancerType, scheme}`, both immutable at creation; tags are the only in-place surface (M22.1 SSM precedent). Instead, the guard was live-proven: a network plan pinning `sslPolicy` → recorded error verbatim: `jarvis-1784289040-nlb: attribute(s) sslPolicy are ALB-only (HTTP semantics) and cannot apply to loadBalancerType: network — remove them or use loadBalancerType: application (fail-closed, no silent drop)` — exit 3, `applied: false`, zero mutating calls, LB live-verified untouched |
| 4   | **Replacement update** | ⚠️ classification + refusal proven; execution justified-deferred | `loadBalancerType` network→application planned `replace` ("present but immutable attribute drifted — will replace (delete+create)"). Gate CLOSED apply → REFUSED, exit 3, zero mutation: `refusing to replace jarvis-1784289040-nlb.aws:elasticloadbalancing:LoadBalancer: immutable attribute drift requires delete+create (destructive); re-run with the replacement gate open`. Gated execution deferred: the application side of this exact handler is already live-proven (M21.3 ALB) — delete+create here would prove nothing new for ~$0 of signal |
| 5   | **Drift**              | ✅     | Out-of-band `aws elbv2 delete-load-balancer` → name released <5s → read-only plan classified `create` ("absent — will create", absence drift) → apply recreated; `active` in ~115s with **new ARN** `…/net/jarvis-1784289040-nlb/c76a9c364be06406` (was `…/f9502a14c778e5ae`), Type/Scheme/tags correct |
| 6   | **Controlled failure** | ✅     | `iap:managed` stripped via `aws elbv2 remove-tags` → destroy REFUSED, exit 3, LB untouched (still `active`): `refusing to delete jarvis-1784289040-nlb.aws:elasticloadbalancing:LoadBalancer: not tagged iap:managed=true (managed-only destroy)` |
| 7   | **Recovery**           | ✅     | Tag restored via add-tags → destroy plan classifies "managed — will delete"; non-destroy dry-run `no-op`, `errors: []`, exit 0 |
| 8   | **Destroy**            | ✅     | DeleteLoadBalancer via destroy apply, `errors: []`, exit 0; describe → LoadBalancerNotFound on first poll (<5s) |
| 9   | **Cleanup verify**     | ✅     | `describe-load-balancers --names jarvis-1784289040-nlb` → `LoadBalancerNotFound`; read-only plan → "absent — will create"; run-scoped tag sweep (`iap:managed=true` filtered to `1784289040`) → 0 matches; elbv2 name check for `*1784289040*` → none. **Zero run-scoped orphans.** |

**Bugs:** none — the handler behaved to spec throughout: the network branch's tag-only read,
the ALB-only-attribute fail-closed guard, immutable type/scheme replacement classification,
and managed-only destroy all matched the mock-tested contract (`test/m22.5-nlb.test.ts`
including the ALB regression canary stayed green, 7/7).

**Honest scope notes:** same target type as the M21.3 ALB — this run proves only the
`loadBalancerType: network` branch, not a new handler. Standalone LB scope: no listeners, no
target groups, no target wiring (the Gateway golden path owns those; `sslPolicy` remains a
listener setting excluded from this handler's projection). Default VPC (ADR-0005), internal
scheme only. No mapping emission for this kind yet — the executor was driven directly with a
hand-built ProviderPlan. Sibling M22.5 runs were live in the same account/region in parallel,
so the region-global `live:sweep` was NOT run here — cleanup verification was run-scoped by
run id (the 34 other `iap:managed` ARNs in the region belong to sibling runs and were never
touched); the coordinator runs the global sweep after the wave.

**Cost: ~$0.03** conservative (~$0.027/h NLB-hour × ~1h budgeted; actual live time ~25 min
total across the two LB instances — the original and the drift-recreate both counted — and
zero LCU traffic). Running total after this service: **~$0.12/$25** (baseline ~$0.09 entering
this run; final wave figure reconciled at closeout, Cost Explorer actuals lag ~24h).
