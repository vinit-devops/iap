# M22.2 — Amazon Timestream (LiveAnalytics Database + Table): LIVE RUN EVIDENCE (service 2 of 4)

**Date:** 2026-07-17 · **Status:** DEFERRED — account not onboarded to Timestream LiveAnalytics;
zero resources created, handlers remain mock-proven 11/11. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `infraasprompt-1784279605`.

Pre-flight PASSED (6/6, budget alarm `iap-live-runs` under the $25 ceiling). The run then hit
AWS's 2025 onboarding closure for Timestream LiveAnalytics on the FIRST call — the service
rejects non-onboarded accounts at the SDK's `DescribeEndpoints` discovery step, before any
Timestream API can execute. Verbatim rejection (read-only `list-databases` probe via CLI):

> An error occurred (AccessDeniedException) when calling the DescribeEndpoints operation:
> Only existing Timestream for LiveAnalytics customers can access the service. Reach out to
> AWS support, for more information.

The same closure was reproduced through the real `AwsExecutor`: a read-only `plan()` for
`infraasprompt-1784279605-db` (`aws:timestream:Database`) + `infraasprompt-1784279605-tbl`
(`aws:timestream:Table`) FAILED CLOSED (exit 3, zero mutating calls) with the SDK's discovery
wrapper: `The operation to discover endpoint failed. Please retry, or provide a custom
endpoint and disable endpoint discovery to proceed.` No workaround (region-shopping, custom
endpoint) was attempted — the closure is account-level and per-run policy the honest outcome
is deferral, not evasion.

## The 9 lifecycle tests

| #   | Test                   | Result | Evidence |
| --- | ---------------------- | ------ | -------- |
| 1–9 | Create → Cleanup verify | ⚠️ DEFERRED (justified) | The account cannot reach ANY `timestream-write` operation — every lifecycle step (CreateDatabase onward) is unreachable behind the onboarding gate. Not a silent skip: rejection captured verbatim above; executor fail-closed live-proven; handlers stay mock-proven 11/11 (`packages/deploy-aws/test/m22.2-timestream.test.ts`, re-run green 2026-07-17) |

**Nothing created, verified:** only read/describe calls were ever issued (probe + gated
`plan()`); `resourcegroupstaggingapi get-resources --tag-filters Key=iap:managed,Values=true`
returned zero ARNs containing `1784279605`. **Zero run-scoped orphans.** (`live:sweep` is
region-global and was NOT run — sibling M22.2 runs execute in parallel; the coordinator sweeps
globally after the wave.)

**Bugs:** none reachable — no handler code path executed against live AWS.

## Honest scope notes

- The desired-gated `kmsKeyId` no-drift behaviour, the `databaseName` gated replace, and live
  retention reconciliation remain proven only against mocks for this service.
- No mapping emission for the timeseries Database class yet — per-branch outputs grammar gap
  plus `databaseName` sibling-reference wiring, deferred like M22.1 apigatewayv2; this run was
  an executor-driven synthetic plan by design.
- The wave's roadmap-mandated live replacement execution is DynamoDB's, so no replacement
  coverage is lost to this deferral.

**Cost: $0.00** (no resources, no data ingested). Running total finalized at closeout;
M22.1 baseline ~$0.08/$25.
