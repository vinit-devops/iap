# M23.2 — AWS ECR Repository (container Registry): LIVE RUN EVIDENCE (service 2 of 6)

**Date:** 2026-07-18 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `infraasprompt-1784345751`.

One ECR repository `infraasprompt-1784345751-repo` (`imageTagMutability MUTABLE`, `scanOnPush true`,
`encryptionType AES256`) created, driven through all nine lifecycle tests, and deleted with
`force:true`. The repository name **is** the identity (plan resourceId); the canonical output is
`repositoryArn`, with `repositoryUri` as the push/pull endpoint. Encryption posture is create-only
on AWS (ADR-0006): AES256→KMS drift classifies as a gated `replace` (delete+create), never
`update`; `imageTagMutability` and `scanOnPush` are mutable in place. This is a **cut-order-lean**
run — an empty repository is effectively free, so create → 9 tests (incl. the destructive
replacement executed live) → teardown ran in one tight sitting.

## Workload

`aws:ecr:Repository` applied via the executor with a hand-built ProviderPlan
(`--confirm` equivalent = `apply: true`), all resources tagged `iap:managed=true` / `iap:planId` /
`iap:resourceId`. Handler calls: CreateRepository (encryption + scanning + tags inline),
PutImageTagMutability / PutImageScanningConfiguration for in-place updates, TagResource to
re-assert ownership, DeleteRepository `force:true` for teardown. No mapping emission for the
container-Registry kind yet (grammar gap, deferred like M22.1/M22.2/M22.3).

## The 9 lifecycle tests

| #   | Test                   | Result | Evidence |
| --- | ---------------------- | ------ | -------- |
| 1   | **Create**             | ✅     | CreateRepository → ARN `arn:aws:ecr:eu-central-1:REDACTED:repository/infraasprompt-1784345751-repo`, URI `REDACTED.dkr.ecr.eu-central-1.amazonaws.com/infraasprompt-1784345751-repo`. Live-verified `imageTagMutability MUTABLE`, `encryptionType AES256`, `scanOnPush true`, and all three tags (`iap:managed`/`iap:planId`/`iap:resourceId`). exit 0. |
| 2   | **No-op**              | ✅     | Immediate re-apply → `no-op`, `applied:false`, zero mutating calls, exit 0 |
| 3   | **Safe update**        | ✅     | `imageTagMutability` MUTABLE→IMMUTABLE planned `update`, applied (PutImageTagMutability), live-verified `IMMUTABLE`; reverted IMMUTABLE→MUTABLE the same way, live-verified `MUTABLE` (both directions recorded) |
| 4   | **Replacement update** | ✅     | `encryptionType` AES256→KMS planned `replace` ("present but immutable attribute drifted — will replace (delete+create)"). Gate CLOSED → REFUSED, exit 3, zero mutation (repo stayed AES256): `refusing to replace infraasprompt-1784345751-repo: immutable attribute drift requires delete+create (destructive); re-run with the replacement gate open`. **Execution DONE** (cheap, near-instant): with the gate open, DeleteRepository→CreateRepository produced a new KMS-encrypted repo (AWS-managed key `arn:aws:kms:eu-central-1:REDACTED:key/…`), tags re-asserted, `applied:true`, exit 0. This KMS repo became the destroy target. |
| 5   | **Drift**              | ✅     | Out-of-band `aws ecr put-image-scanning-configuration … scanOnPush=false` → read-only plan detected `update`; live still `false` after plan (read-only proven); apply reconciled → live `true` |
| 6   | **Controlled failure** | ✅     | `iap:managed` stripped via `aws ecr untag-resource --tag-keys iap:managed` → destroy REFUSED, exit 3, repo untouched: `refusing to delete infraasprompt-1784345751-repo: not tagged iap:managed=true (managed-only destroy)` |
| 7   | **Recovery**           | ✅     | Tag restored (tag-resource) → destroy plan classifies "managed — will delete"; non-destroy dry-run `no-op`, `errors: []`, exit 0 |
| 8   | **Destroy**            | ✅     | `destroy` apply (gate + confirm) → DeleteRepository `force:true`, `applied:true`, `errors: []`, exit 0. Near-instant (empty repo). |
| 9   | **Cleanup verify**     | ✅     | describe-repositories → `RepositoryNotFoundException`; re-plan → `create` ("absent — will create"); run-scoped tag sweep (`iap:managed=true`, `ecr:repository`, filtered to `1784345751`) → `[]`. **Zero run-scoped orphans.** |

## Real bugs caught by live execution (mock tests could not)

None. The handler behaved to spec end to end on real AWS: name-as-identity create with inline
encryption/scanning/tags, in-place mutability + scan-on-push updates (both directions), the
immutable-encryption `replace` classification + gate-closed refusal AND the live destructive
delete+create, out-of-band drift read-only detection + reconcile, managed-only destroy refusal,
and `force:true` teardown. The existing mock suite already covered these paths; the live run
confirmed the same behaviour against the service, including the AES256→KMS replacement the mock
cannot exercise for real. `src/ecr.ts` and `test/m23.2-ecr.test.ts` were left unchanged (no bug
protocol invoked).

## Honest scope notes

- **No mapping emission** for the container-Registry kind — the run drove the executor directly
  with a hand-built ProviderPlan (same gap as M22.1/M22.2/M22.3).
- **Cut-order-lean:** a single empty repository, no images pushed. `force:true` teardown was
  therefore untested against actual image content (the un-forced-delete-fails path is covered by
  mock only); the flag is present and the delete succeeded.
- **KMS key:** the replacement used the AWS-managed default ECR KMS key (no customer key pinned).
  The handler's desired-gated kmsKey comparison means an unpinned plan does not read the
  AWS-assigned key as drift (M22.1/M22.2 lesson) — confirmed: post-replacement no-op held.
- **Parallel wave:** 5 sibling M23.2 service runs were live in the same account/region
  concurrently, so the region-global `live:sweep` was NOT run here — cleanup verification was
  **run-scoped by run id** (`1784345751`); the coordinator runs the global sweep after the wave.

## Cost

**~$0.00 for this service run.** ECR has no per-hour repository charge; cost is storage +
data-transfer + scan, all of which were zero (no image ever pushed, repository lifecycle spanned
minutes). Even the AES256→KMS replacement created no billable KMS key (AWS-managed default ECR
key, no dedicated CMK). At closeout (M24.4) this reconciles to ~$0.

Prior baselines this wave: M23.2 service 1 costs sum at wave closeout across all 6 services
(5 sibling runs live in parallel). Well under the <$25 roadmap ceiling.
