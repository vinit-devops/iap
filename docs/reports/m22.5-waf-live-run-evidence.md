# M22.5 — AWS WAFv2 WebACL (edge posture): LIVE RUN EVIDENCE (service 6 of 6)

**Date:** 2026-07-17 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `infraasprompt-1784289041`.

One REGIONAL WebACL `infraasprompt-1784289041-acl` (DefaultAction Allow, **zero rules** — v1 posture,
VisibilityConfig with CloudWatch metrics + sampled requests, MetricName = resource id). This run
proved WAF's **optimistic-concurrency LockToken discipline live**: the token rotates on every
mutation, and every mutation (three updates + the delete) fetched it fresh via GetWebACL —
zero `WAFOptimisticLockException` across the run.

| #   | Test                   | Result | Evidence                                                                                   |
| --- | ---------------------- | ------ | -------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅     | CreateWebACL near-instant; ARN `…:regional/webacl/infraasprompt-1784289041-acl/6a4967d3-…` recorded; live GetWebACL confirmed Allow default, `Rules: []`, VisibilityConfig as spec'd; `iap:managed`/`iap:planId`/`iap:resourceId` all verified via list-tags-for-resource |
| 2   | **No-op**              | ✅     | Immediate re-plan + re-apply → `no-op`, "present and converged", zero mutation             |
| 3   | **Safe update**        | ✅     | defaultAction `allow`→`block` planned `update`; UpdateWebACL with a FRESH LockToken carrying the full config (Rules + VisibilityConfig passed back verbatim — UpdateWebACL is a full PUT); live GetWebACL → `DefaultAction: Block`, token now `02bfc13e-…`. Reverted `block`→`allow` the same way: second fresh fetch, live → `Allow`, token rotated to `61ef067d-…`. **Two live LockToken cycles proven, both directions recorded** |
| 4   | **Replacement update** | ✅ (honest) | scope REGIONAL→CLOUDFRONT. Handler caveat says cross-scope lookup lists only the desired scope, so the live read may classify absent→create rather than replace. Live is stricter still: in eu-central-1 `ListWebACLs(Scope=CLOUDFRONT)` is rejected by the service — `WAFInvalidParameterException: Error reason: The scope is not valid., field: SCOPE_VALUE, parameter: CLOUDFRONT` (CLOUDFRONT scope exists only via us-east-1). Read-only `plan()` propagated the throw before any classification; gate-closed `apply()` recorded it per the never-throw contract (`errors[1]`, exit 3, zero mutation). **Gate stayed closed throughout; nothing was created in CLOUDFRONT scope**; the REGIONAL ACL was untouched. The handler's ARN-decode replace path remains mock-proven (`test/m22.5-waf.test.ts`) |
| 5   | **Drift**              | ✅     | Out-of-band `aws wafv2 update-web-acl` flipped DefaultAction to Block (lock token fetched via get-web-acl, full config passed; CLI returned NextLockToken `79388bb0-…`) → read-only plan classified `update`; live value still Block after the plan (read-only proven) → apply reconciled with a third fresh-token cycle → live `Allow`, re-plan `no-op` (converged) |
| 6   | **Controlled failure** | ✅     | `iap:managed` stripped via untag-resource → destroy REFUSED, exit 3, ACL untouched (still listed): "refusing to delete infraasprompt-1784289041-acl.aws:wafv2:WebACL: not tagged iap:managed=true (managed-only destroy)" |
| 7   | **Recovery**           | ✅     | Tag restored via tag-resource → destroy plan classifies "managed — will delete"; non-destroy dry-run `no-op`, `errors: []`, exit 0 |
| 8   | **Destroy**            | ✅     | Fresh LockToken via GetWebACL → DeleteWebACL, `errors: []`, exit 0                          |
| 9   | **Cleanup verify**     | ✅     | list-web-acls (REGIONAL) name match → `[]`; run-scoped tag sweep (`iap:managed=true` filtered to `1784289041`) → 0 matches; destroy re-plan → "absent — nothing to do". **Zero run-scoped orphans.** |

**Bugs:** none — the handler behaved to spec throughout, including the fresh-LockToken-per-mutation
discipline (three update cycles + delete, no stale-token failure even after an out-of-band CLI
mutation rotated the token) and the full-PUT UpdateWebACL semantics (Rules/VisibilityConfig
carried back verbatim, nothing erased).

**Honest scope notes:** zero rules in v1 — managed rule groups (AWSManagedRulesCommonRuleSet etc.)
are later scope. No ALB association — the roadmap's "WAF derived from a public Gateway" story is
future mapping scope; this run proved the standalone ACL only. Cross-scope replacement could not
be exercised live from eu-central-1 (test 4): the service rejects CLOUDFRONT-scope calls outside
us-east-1, so the live run proves the fail-closed behaviour (thrown from plan, recorded error from
apply), not the delete+create path — that path stays mock-proven. No mapping emission for the WAF
kind yet — this run drove the executor directly with a hand-built ProviderPlan. Sibling M22.5 runs
were live in the same account/region in parallel, so the region-global `live:sweep` was NOT run
here — cleanup verification was run-scoped by run id; the coordinator runs the global sweep after
the wave.

**Cost: ≈$0.01** (WebACL $5.00/mo prorated over a ~15-minute lifetime; zero rules, zero request
volume). Wave running total finalized at closeout; baseline entering M22.5 ~$0.09/$25.
