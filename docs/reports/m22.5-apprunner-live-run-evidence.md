# M22.5 — AWS App Runner: LIVE RUN EVIDENCE (service 3 of 6)

**Date:** 2026-07-18 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `jarvis-1784338128`.

One fully-managed App Runner service `jarvis-1784338128-svc` from the public hello-app-runner
container image (`public.ecr.aws/aws-containers/hello-app-runner:latest`, ECR_PUBLIC), port
8000, Cpu 256 / Memory 512. The CLI drove create → live HTTP 200 → no-op → in-place update →
out-of-band drift reconcile → managed-only destroy → cleanup end to end. **Headline: the live
`ServiceUrl` served HTTP 200** (the hello page) once the service reached `RUNNING`. This was a
FRESH clean re-run after two prior attempts died on token expiry (prior service torn down; this
run started from an empty name space).

## Workload

`aws:apprunner:Service` `jarvis-1784338128-svc` `{ image: hello-app-runner:latest, port: 8000,
cpu: 256, memory: 512 }`, AutoDeploymentsEnabled false, created with `--confirm` and tagged
`iap:managed=true` / `iap:planId=jarvis-1784338128` /
`iap:resourceId=jarvis-1784338128-svc.aws:apprunner:Service`. Driven directly with a hand-built
ProviderPlan (no mapping emission for this kind yet). ARN
`…:service/jarvis-1784338128-svc/93f7546a2bf54f87a4d08706fb683268`.

## The 9 lifecycle tests

| #   | Test                   | Result | Evidence                                                                                   |
| --- | ---------------------- | ------ | -------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅     | CreateService → ARN `…/jarvis-1784338128-svc/93f7546a2bf54f87a4d08706fb683268`; polled `OPERATION_IN_PROGRESS`→`RUNNING` in **170s** (budget 10 min, 20s poll); all three tags present via `list-tags-for-resource`. **BONUS: `curl https://<ServiceUrl>/` → HTTP 200** (hello-app-runner page served live) |
| 2   | **No-op**              | ✅     | Immediate re-plan → `no-op` "present and converged"; gate-closed dry-run → `no-op`, `errors: []`, exit 0. Read-back Cpu/Memory `256`/`512` matched desired exactly — **no phantom drift** |
| 3   | **Safe update**        | ✅     | Memory `512`→`1024`: plan → `update` "present but drifted — will reconcile"; apply → `UpdateService` in place, `applied: true`; polled back to `RUNNING` in 169s; live `InstanceConfiguration` verified `Cpu 256 / Memory 1024` |
| 4   | **Replacement update** | ⚠️ justified N/A (ADR-0006) | Every projection key (image/port/cpu/memory) reconciles in place via `UpdateService` — the handler declares **no** `immutableProjectionKeys`, so drift can never classify `replace`. Proven live by Test 3 (memory drift → `update`, never delete+create) and by the mock canary (`test/m22.5-apprunner.test.ts` "image drift classifies as update") |
| 5   | **Drift**              | ✅     | Out-of-band `aws apprunner update-service` Port `8000`→`8080` (waited for the op → `RUNNING`, live Port `8080` confirmed) → read-only `iap plan` classified `update` "present but drifted — will reconcile" (zero mutation) → reconcile apply → polled `RUNNING` in 191s, Port restored to `8000` |
| 6   | **Controlled failure** | ✅     | `iap:managed` stripped via `untag-resource` → `destroy` REFUSED, exit 3, service untouched: `refusing to delete jarvis-1784338128-svc.aws:apprunner:Service: not tagged iap:managed=true (managed-only destroy)` |
| 7   | **Recovery**           | ✅     | Tag restored via `tag-resource` → destroy plan classifies "managed — will delete"; `errors: []`, exit 0 |
| 8   | **Destroy**            | ✅     | `DeleteService` via destroy apply, exit 0; service reached `DELETED`/absent |
| 9   | **Cleanup verify**     | ✅     | `list-services` → name absent; run-scoped tag sweep (`iap:managed=true` filtered to `1784338128`) → **0 matches. Zero run-scoped orphans.** |

**Bugs:** none. The handler behaved to spec throughout — name→ARN resolution via paginated
`ListServices`, ARN-driven describe/update/delete, OPERATION_IN_PROGRESS serialization handled
by the driver's waits (never an in-handler spin), in-place reconcile of all four projection
keys, and the managed-only destroy refusal all matched the mock-tested contract
(`test/m22.5-apprunner.test.ts`, 7/7 green).

**Phantom-drift verdict (Cpu/Memory normalization):** **NONE.** App Runner returns `Cpu`/`Memory`
as the same short numeric strings the handler sends (`256`/`512`/`1024`), so the read projection
matched the desired projection with no normalization gap. The live `no-op` in Test 2 (and the
clean post-update `no-op` state) proves it — a normalization phantom would have classified
`update` on an unchanged service. The bug protocol was therefore not triggered; no handler change
was needed.

## Honest scope notes

Public hello-app-runner image, ECR_PUBLIC, AutoDeploymentsEnabled false — no private ECR, no
VPC connector, no custom domain, no auto-scaling config (defaults). App Runner serializes
operations: every mutating op (create/update/reconcile) runs `OPERATION_IN_PROGRESS` for
~170–190s and the driver **waits between ops** — the handler surfaces `InvalidStateException`
honestly rather than retrying (mock-proven). The `DELETED` status lingers by name in
`ListServices` briefly after deletion; the handler reads `DELETED` as absent. No mapping
emission for this kind — the executor was driven directly with a hand-built ProviderPlan. This
was a clean re-run after two token-expiry interruptions; it started from an empty name space.
Sibling M22.5 runs were live in the same account/region in parallel, so cleanup verification was
**run-scoped by run id** (filtered to `1784338128`); no region-global `live:sweep` was run here
(the coordinator runs the global sweep after the wave). **Tests 6–9 were completed by the
coordinator via the handler driver after the agent's poller stalled; teardown was verified
zero-orphan.**

## Cost

One App Runner service, ~4 provisioning/update cycles (~170–190s each) plus brief RUNNING time;
total live wall-clock well under 1 hour, near-zero request traffic (a single curl). App Runner
bills provisioned instance vCPU/memory-hours plus active request compute. **Est. cost: ~$0.01**
for this service run. Running total after this service: **well within the <$25 roadmap ceiling**
(final wave figure reconciled at closeout, M24.4 — Cost Explorer actuals lag ~24h).
