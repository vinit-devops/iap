# M22.2 — AWS Backup (vault + plan): LIVE RUN EVIDENCE (service 4 of 4)

**Date:** 2026-07-17 · **Status:** COMPLETE — real AWS, fully torn down. **Account:** REDACTED ·
**Region:** eu-central-1 · **Profile:** `REDACTED_AdministratorAccess` · **Run id:** `infraasprompt-1784279607`.

Backup posture pair: `aws:backup:BackupVault` (identity-only projection) +
`aws:backup:BackupPlan` (name-resolved generated id, single `daily` rule). The executor orders
resources alphabetically by logicalId, so create/destroy were driven as two sequential
single-resource plans (vault → plan on create; plan → vault on destroy).

| #   | Test                   | Result | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | ---------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Create**             | ✅     | Vault ARN `…backup-vault:infraasprompt-1784279607-vault`; plan ARN `…backup-plan:d0657e35-2c18-4150-8e3c-7b37a86f3825`, v1 `ZWJhYWJk…`; `get-backup-plan` shows `daily` rule → vault, `cron(0 5 * * ? *)`, DeleteAfterDays 7; all three `iap:*` tags on both ARNs                                                                                                                                                                        |
| 2   | **No-op**              | ✅     | Immediate re-plan → both `no-op` (vault projection is identity-only — a present vault always reads converged; documented handler contract)                                                                                                                                                                                                                                                                                               |
| 3   | **Safe update**        | ✅     | retentionDays 7→14 → plan `update` → UpdateBackupPlan created NEW VersionId `NWY2YmE2…` in place (same BackupPlanId); superseded v1 kept in `list-backup-plan-versions` with a DeletionDate; live rule verified DeleteAfterDays 14                                                                                                                                                                                                       |
| 4   | **Replacement update** | ⚠️ N/A | Justified: vault has no non-identity attributes; every plan key (vaultName/schedule/retention) mutates in place via UpdateBackupPlan — no immutable projection keys on either type (ADR-0006). The wave's mandated live replacement was exercised by DynamoDB                                                                                                                                                                            |
| 5   | **Drift**              | ✅     | Out-of-band `aws backup update-backup-plan` → `cron(0 6 * * ? *)` (v3 `MzFhOWFl…`); read-only plan → `update`; reconcile apply → v4 `YWRkMzdl…` back to `cron(0 5 * * ? *)`; re-plan `no-op`                                                                                                                                                                                                                                             |
| 6   | **Controlled failure** | ✅     | `untag-resource` stripped `iap:managed` from the plan → destroy REFUSED: `refusing to delete infraasprompt-1784279607-plan.aws:backup:BackupPlan: not tagged iap:managed=true (managed-only destroy)`, exit 3                                                                                                                                                                                                                            |
| 7   | **Recovery**           | ✅     | `tag-resource` restored `iap:managed=true` → dry destroy classifies both `delete` / "managed — will delete"                                                                                                                                                                                                                                                                                                                              |
| 8   | **Destroy**            | ✅     | Plan deleted BEFORE vault (empty vault — no recovery points, so DeleteBackupVault succeeded); both exits 0, `errors: []`                                                                                                                                                                                                                                                                                                                 |
| 9   | **Cleanup verify**     | ✅     | `get-backup-plan` → ResourceNotFoundException; name no longer resolves via ListBackupPlans; `describe-backup-vault` → AccessDeniedException (the Backup service's documented absent-vault shape — live confirmation of the handler's dual-token VAULT_NOT_FOUND detection); executor re-plan → both "absent — will create"; run-scoped tag sweep (`iap:managed=true` ARNs filtered to `1784279607`) → zero. **Zero run-scoped orphans.** |

**UpdateBackupPlan version history:** 4 versions across the run (create → safe update →
out-of-band drift → reconcile); every update mints a new VersionId on the same BackupPlanId
and the superseded version stays listed with a DeletionDate — genuine in-place mutation,
confirming replacement-N/A.

**Bugs:** none.

**Honest scope notes:** backup posture emission from the mapping is deferred — per-branch
outputs grammar gap + `vaultName` sibling-reference wiring (the plan here referenced the vault
by constructed name, not a resolved output). No BackupSelection was created and no backup job
ran — nothing was actually backed up; these are posture resources only, so the fail-closed
DeleteBackupVault-with-recovery-points path stays mock-proven. `pnpm run live:sweep` was NOT
run: it is region-global and sibling M22.2 runs (DynamoDB, Timestream, KMS) were live in
parallel — the coordinator sweeps after the wave; the cleanup verify above is run-scoped.

**Cost: $0.00** — empty vault, no backup jobs, backup plans are free. Running total finalized
at closeout; M22.1 baseline ~$0.08/$25.
