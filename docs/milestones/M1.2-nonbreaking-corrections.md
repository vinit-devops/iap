# Milestone M1.2 — Non-Breaking Corrections (Gap Report §7)

**Phase:** 1 — IaP v1 Stabilization
**Milestone:** M1.2
**Status:** Completed
**Date:** 2026-07-10

## Implemented

Schema (`spec/schema/iap-v1.schema.json`; all annotation-level, no validation behavior changes):

- `relationshipEdge.access` and `ruleEdge.access` now carry `default: read-write` (matches ch. 4 §4.4 prose).
- `$defs/common/resilience` split into `resilienceBackupRequired` (used by `Database`, `Volume`) and `resilienceBackupPreferred` (used by `ObjectStore`) — the per-kind backup defaults from ch. 3 §3.2 are now machine-readable, as required by M1.1's materialization rule 6.
- `x-iap-presence-semantic: true` annotated on `Service.spec.healthCheck` and `Queue.spec.deadLetter` (ch. 1 §1.5.1 rule 4).
- `x-iap-default-when: {enabled: true}` annotated on `Queue.spec.deadLetter.maxReceives` (conditional default, ch. 1 §1.5.1 rule 5).

Prose:

- Ch. 4 §4.6: explicit **v1 non-goal** for `replicatesTo` failover semantics (writable side after failure, promotion, failback — reserved for a future IEP).
- Ch. 7 §7.5: `require` autofix eligibility restricted to `equals` leaves and `allOf` conjunctions of them; all other operators report-only.
- Ch. 8 §Phase-1 and ch. 24 CV-6: annotation vocabulary lists extended with the two new keywords.

Code kept in lockstep: `X_IIS_ANNOTATION_KEYWORDS` in `@iap/model` and the harness's registered vocabulary now include both new keywords (the drift tests enforce schema/embedded equality; `pnpm run verify` green).

## Files changed

Modified: `spec/schema/iap-v1.schema.json`, `spec/chapters/{04,07,08,24}-*.md`, `packages/model/src/index.ts`, `tests/conformance/run.mjs` (+ re-synced `packages/model/schemas/`). Created: this document.

## Tests added

None new; existing 33 unit tests + 18 harness checks re-run green, proving the corrections are non-breaking (all examples and cases still produce declared outcomes).

## Conformance status

Green. No document that was valid before is invalid now, and vice versa (annotations only).

## Architecture decisions

Per-kind defaults via def-splitting (two explicit `$defs`) rather than `$ref`-sibling default overrides — keeps every default discoverable by walking `$defs`, which the canonicalizer and LSP rely on.

## Specification gaps

Gap report §7 items 1–3, 5–6 now closed (item 4 — conformance cases — is M1.3).

## Security findings

None.

## Known limitations

`x-iap-default-when` is a v1-scoped annotation with exactly one instance; a general conditional-default facility is deliberately NOT specified (YAGNI — revisit only via IEP if a second instance appears).

## Next milestone

M1.3 — conformance coverage expansion; M1.4 — additional official examples (parallel).
